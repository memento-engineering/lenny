# GENERATED by tool/generate_grid_assets.dart — do not edit.
# Source of truth: the `grid:` block in packages/leonard_grid_assets/pubspec.yaml.
#
# The MCP-shaped compatibility mirror of that block, authored in
# the Dart/Flutter "Packaged AI Assets" format (per
# flutter.dev/go/packaged-ai-assets — the
# package:extension_discovery `extension/mcp/config.yaml`
# shape). It carries only MCP concepts; grid-only kind,
# selector, and non-MCP delivery legs stay in the block and in
# lib/src/assets/grid_asset_pack.dart.
name: leonard_grid_assets
version: 1

# Rubric prose — one resource per committee lane.
resources:
  - id: plain-dart-flutter
    description: "Level 0 reference for tests that prove behavior without Leonard live driving."
    path: station_overlay/claude/skills/test-with-leonard/references/plain-dart-flutter.md
    visibility: public
  - id: widget
    description: "Level 1 reference for testing Leonard extensions and observations without a device."
    path: station_overlay/claude/skills/test-with-leonard/references/widget.md
    visibility: public
  - id: scripted-device
    description: "Level 2 reference for scripted tests against a simulator or emulator."
    path: station_overlay/claude/skills/test-with-leonard/references/scripted-device.md
    visibility: public
  - id: hardware
    description: "Level 3 reference for tests whose behavior exists only on physical hardware."
    path: station_overlay/claude/skills/test-with-leonard/references/hardware.md
    visibility: public
  - id: oracle
    description: "Level 4 reference for scenarios graded by an independent oracle."
    path: station_overlay/claude/skills/test-with-leonard/references/oracle.md
    visibility: public
  - id: mutation
    description: "Reference for improving assertion quality through mutation testing."
    path: station_overlay/claude/skills/test-with-leonard/references/mutation.md
    visibility: public
  - id: triage
    description: "Reference for diagnosing a failed Leonard test run."
    path: station_overlay/claude/skills/test-with-leonard/references/triage.md
    visibility: public

# Skills — the agentskills SKILL.md assets the station_overlay
# vends. `audience: operator` is the DENY-list a build agent brief
# reads through `operatorSkillIds`; the claude leg is mirrored here,
# and every other leg lives in the block.
skills:
  - id: test-with-leonard
    description: "Route Dart and Flutter test authoring to the cheapest proving level and load only the selected Leonard reference."
    path: station_overlay/claude/skills/test-with-leonard/SKILL.md
    visibility: public
    audience: agent
station_overlay:
  mappings:
    claude: .claude
