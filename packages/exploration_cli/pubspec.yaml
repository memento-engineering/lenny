name: exploration_cli
description: CLI frontend for the Flutter Exploration Agent.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  exploration_agent: any
  path: ^1.9.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
