name: leonard_cli
description: CLI frontend for Leonard.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  leonard_agent: any
  path: ^1.9.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
