name: exploration_riverpod
description: Reference plugin (exploration_riverpod) for the Flutter Exploration Agent.
publish_to: none
resolution: workspace

environment:
  flutter: ">=3.41.0"
  sdk: ^3.11.0

dependencies:
  exploration_flutter: any
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  genesis_perception:
    git:
      url: git@github.com:memento-engineering/genesis.git
      ref: 41ec0ec
      path: packages/perception

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.0
