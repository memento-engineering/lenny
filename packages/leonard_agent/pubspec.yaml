name: leonard_agent
description: Web-compatible harness library.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  http: ^1.2.0
  json_schema: ^5.1.7
  meta: ^1.16.0
  vm_service: ^14.2.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
