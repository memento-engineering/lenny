name: exploration_router
description: "Reference plugin: Router/Navigator observation and navigate tool."
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  exploration_flutter: any
  flutter:
    sdk: flutter
  genesis_perception:
    git:
      url: git@github.com:memento-engineering/genesis.git
      ref: 41ec0ec
      path: packages/perception
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.0
