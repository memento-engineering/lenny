# leonard_flutter_test

Test utilities for applications and extensions built with `leonard_flutter`.

Add this package to `dev_dependencies`, then import the stable barrel:

```dart
import 'package:leonard_flutter_test/leonard_flutter_test.dart';
```
