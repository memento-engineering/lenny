# Changelog

## 0.1.1

- Declare the direct `flutter` SDK dependency so Flutter-aware tooling runs
  this test-support package with the Flutter test runner.

## 0.1.0

- Initial release: test-only VM-service and observation-equivalence helpers
  extracted from `leonard_flutter`.
