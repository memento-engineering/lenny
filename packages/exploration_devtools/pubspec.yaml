name: exploration_devtools
description: DevTools extension for the Flutter Exploration Agent.
publish_to: none
resolution: workspace

environment:
  flutter: ">=3.41.0"
  sdk: ^3.11.0

dependencies:
  devtools_extensions: ^0.4.0
  dtd: ^4.0.0
  exploration_agent: any
  flutter:
    sdk: flutter
  http: ^1.2.0
  json_rpc_2: ^3.0.2
  vm_service: ^14.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.0
  test: any
