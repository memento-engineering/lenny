import 'package:exploration_devtools/exploration_devtools.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('package loads', () {
    expect(packageName, equals('exploration_devtools'));
  });
}
