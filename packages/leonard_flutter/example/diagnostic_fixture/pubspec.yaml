name: diagnostic_fixture
description: Sample-app fixture for the connect-time semantics diagnostic.
publish_to: none
resolution: workspace

environment:
  flutter: ">=3.41.0"
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  leonard_flutter: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.0
