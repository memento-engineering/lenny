name: leonard_dio
description: Reference extension (dio) for Leonard.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  dio: ^5.7.0
  flutter:
    sdk: flutter
  genesis_perception: ^0.1.1
  leonard_flutter: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.0
