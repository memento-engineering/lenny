name: leonard_riverpod
description: Reference extension (leonard_riverpod) for Leonard.
version: 0.2.1
repository: https://github.com/memento-engineering/lenny
resolution: workspace

environment:
  flutter: ">=3.41.0"
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  genesis_perception: ^0.2.0
  leonard_flutter: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.0
