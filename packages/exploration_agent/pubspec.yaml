name: exploration_agent
description: Web-compatible harness library.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.16.0
  vm_service: ^14.2.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
