{"_type":"issue","id":"lenny-cx6.18","title":"Perception-action loop driver with budgets and failure handling","description":"The orchestrator. Per turn: stabilize → capture core → capture plugin fragments → diff → build prompt → decide → validate (≤3 retries) → act (host or plugin extension) → notify plugins via onActionExecuted → persist. Enforces per-turn (30s) and per-session (50 turns / 15min) budgets. Implements all PRD §17 failure modes: stability budget expired, invalid action retries, malformed model retries, three-failed-turns → harness_error: agent_stuck, plugin exception isolation + auto-disable after 3 failures, runtime app errors don't end session, VM connection lost preserves trajectory, binding-not-initialized exit, budget exhaustion clean end, voluntary done(reason). PRD §10, §17.","design":"## Approach\n`LoopDriver` is the convergence point of phase 2 — orchestrates one full turn end-to-end and runs the session-level budget machinery. Lives in `exploration_agent`, called from `ExplorationSession` (.11).\n\n**Per-turn ordering (PRD §10, exact 10-step sequence — load-bearing):**\n1. **Stabilize.** Call host's `getStableObservation(policy)` via VmServiceClient (.11/.8). Default policy: action-relative.\n2. **Capture core fragment.** Already merged in step 1's response (host-side); driver just deserializes (.12).\n3. **Capture plugin fragments.** Same — host already called every plugin's `observe()`; driver deserializes.\n4. **Diff.** Call `ObservationDiffer.diff(prev, curr)` (.12). Persist `prev` for next turn.\n5. **Build prompt.** Call `PromptAssembler` (.13) with the **current** merged tool list (regenerated from session's currently-active plugin set — auto-disabled plugins absent).\n6. **Decide.** Compose action schema via `.fromToolList(currentTools)` (.14), call `provider.decide(prompt, schema)`.\n7. **Validate.** `ActionValidator.validate(decision.action, observation, currentTools)` (.17). On reject, retry decide up to 3× per PRD §17 with the rejection spliced back into the prompt.\n8. **Act.** Dispatch to the appropriate VM service extension (host's `core.*` or `ext.flutter.\u003cplugin_namespace\u003e.\u003ctool\u003e`).\n9. **Notify-plugins.** Call `onActionExecuted` on every plugin (host-side via VM extension; driver triggers it).\n10. **Persist.** Append turn record to trajectory writer (.19). Per-turn flush so a crash preserves progress.\n\n**Budgets (PRD §10):**\n- Per-turn: 30s wall-clock. Exceeded → fail the turn.\n- Per-session: 50 turns or 15 minutes wall-clock. Exceeded → clean end with `outcome: budget_exhausted`.\n\n**Failure modes — all of PRD §17 implemented here:**\n- Stability budget expired with plugins still busy → tag observation; not a failure.\n- Invalid action → up to 3 retries (per .17); 4th attempt counts as a **failed turn**.\n- Malformed model output (schema violation) → 1 retry with schema error injected; 2nd failure is a failed turn.\n- **3 consecutive failed turns → end session, `harness_error: agent_stuck`** (write final record).\n- Plugin throws during `observe()` / `busyState()` / `onActionExecuted()` → log, turn proceeds without that plugin's contribution. **3 consecutive plugin failures → auto-disable that plugin only** for the rest of the session (PRD §17). Disabled plugin is removed from the active set the next turn — its tools disappear from the merged list (.13/.14), its observation fragment is no longer requested.\n- Plugin throws during `initialize()` → host marks failed at startup; session continues without it.\n- Runtime error in app under test → captured in observation, doesn't end session.\n- VM service connection lost → end with `harness_error: connection_lost`; trajectory preserved (writer's per-turn flush guarantees this).\n- Binding not initialized → detected at session start by .11; clean error pointing at setup.\n- Voluntary `core.done(reason)` → end session, write final summary, `outcome: done`.\n\n**Plugin auto-disable mechanic.** Driver maintains a `Map\u003cnamespace, consecutiveFailures\u003e` counter. Reset to 0 on a successful plugin observation. On hit 3, mutate session's active-plugin set (the same set .13/.14 read from). The next turn's tool list and observation request reflect the change. Log loudly. Trajectory record captures the disable event.\n\n## Constraints\n- **Web-compatible** per PRD §22: pure Dart, all I/O via VmServiceClient (HTTP) and trajectory writer abstraction (.19). No `dart:io`.\n- Step ordering is **mandatory** — diff before prompt, prompt before decide, validate before act, notify before persist. Don't reorder for \"efficiency\"; the trajectory's coherence depends on this sequence.\n- Budgets enforced at the driver level, not pushed into providers/tools (which see only their own work).\n- Failure modes from PRD §17 are exhaustive — implementing fewer is a spec violation.\n\n## Ruled out\n- Parallelizing decide + validate (e.g., speculatively executing while validating): coherence cost + bug surface vastly outweighs the latency saved.\n- Letting plugins veto a turn: contract has no veto and shouldn't — plugins contribute, they don't drive.\n- Per-plugin failure counts shared across plugins (\"any 3 plugin failures total\"): would punish well-behaved plugins for a misbehaving neighbor; PRD §17 is explicit that auto-disable is per plugin.\n- Ignoring stability budget expiry: PRD says capture-and-tag, not retry — the model is informed via stability metadata and adjusts.\n- Soft-cap clamps on running summary: handled in .13 (rejection path); driver just surfaces the retry.\n\n## Non-obvious notes\n- This is the **convergence point**: depends on .6, .13, .16, .17, .19 (and transitively all of phase 1 and phase 2). Land everything else first; this story stitches it together.\n- The 10-step ordering is reproduced in PRD §10 — keep tests that walk a turn and assert each step ran in order. This catches refactor regressions where someone \"cleans up\" by moving steps.\n- Plugin auto-disable is the v0.5 isolation guarantee — verify with a fault-injecting fake plugin that throws on every `observe()`; assert it's disabled after exactly 3 attempts and the session continues healthy.\n- The `core.done(reason)` tool lives in the core action set (.6); driver inspects the executed action each turn for it.\n- Cross-references: PRD §10 (the canonical loop), §17 (every failure mode), §11.4 (observation budget — driver doesn't enforce, but propagates configured values to the host's getStableObservation call).","acceptance_criteria":"- [ ] Full turn completes against a sample app under all default policies\n- [ ] Per-turn and per-session budgets terminate cleanly\n- [ ] 3 consecutive failed turns ends with harness_error: agent_stuck\n- [ ] Plugin exceptions isolated; 3 consecutive failures auto-disable that plugin only\n- [ ] Lost VM connection ends with harness_error: connection_lost; trajectory preserved\n- [ ] done(reason) ends and writes final summary","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:19Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:50:54Z","dependencies":[{"issue_id":"lenny-cx6.18","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:18Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.18","depends_on_id":"lenny-cx6.13","type":"blocks","created_at":"2026-05-07T09:01:29Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.18","depends_on_id":"lenny-cx6.16","type":"blocks","created_at":"2026-05-07T09:01:30Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.18","depends_on_id":"lenny-cx6.17","type":"blocks","created_at":"2026-05-07T09:01:30Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.18","depends_on_id":"lenny-cx6.19","type":"blocks","created_at":"2026-05-07T09:01:31Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.18","depends_on_id":"lenny-cx6.6","type":"blocks","created_at":"2026-05-07T09:01:28Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":5,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.17","title":"Action validator against merged tool list","description":"Given a candidate action and the current observation, validate: tool name exists in merged tool list (host core + active plugin tools); args match the tool's JsonSchema; for core tools that target semantic node IDs, the node exists, is enabled, and is visible. On rejection, return a structured failure designed to be model-readable. Driver retries the same observation up to 3 times. PRD §10 step 7, §17.","design":"## Approach\n`ActionValidator.validate(action, observation, mergedToolList) → ValidationResult` where the result is `ok | reject(ValidationError)`. Lives in `exploration_agent`, called by the loop driver (.18) between `decide` and `act` (PRD §10 step 7).\n\n**Validation passes, in order:**\n1. **Tool name exists** in the merged tool list (host core + active plugin tools per the session's current plugin set). Unknown tool → `reject(\"unknown tool: \u003cname\u003e. available: \u003clist\u003e\")`.\n2. **Args match the tool's `inputSchema`.** Use the same JSON Schema validator the dynamic schema composer (.14) uses. Schema mismatch → `reject` with the JSON Pointer path of the bad field and a one-line human description (\"missing required field 'node_id'\", \"node_id must be int\").\n3. **For core tools that target semantic node IDs** (`core.tap`, `core.long_press`, `core.enter_text`, `core.scroll_until_visible`, `core.inspect_widget`): look up the node in the current observation's semantics tree. Reject if (a) node missing, (b) node disabled, (c) node off-screen / not visible. Plugin tools target plugin-owned identifiers (provider IDs, route paths, request IDs) per PRD §12.3 — the validator does not introspect those; the plugin's own `inputSchema` is the only schema check, and any deeper validation is on the plugin's tool implementation.\n\n**Rejection messages are model-readable**: structured (`{tool, reason, expected, got}`) so the loop driver (.18) can splice them back into the next prompt for the model's retry attempt. Per PRD §17, up to 3 retries against the same observation before counting a failed turn.\n\n## Constraints\n- **Web-compatible** per PRD §22: pure Dart, no I/O. Schema validation library must not pull in `dart:io`.\n- Validator is **stateless**: same inputs → same result. Required for testability and for the retry loop to make sense (no hidden mutation between attempts).\n- The merged tool list passed in must be the **current** snapshot per turn (consistent with .13/.14 dynamic-tool-list invariant) — not cached. The validator never fetches it; it's an input.\n\n## Ruled out\n- Calling into the host (VM service round-trip) to ask \"is this node valid?\": the observation we already have contains the semantics tree at turn capture time, which is exactly the snapshot the model proposed against. Round-tripping introduces a TOCTOU race — node may be gone by the time we ask.\n- Letting plugins register custom validators for their tools: keeps the contract small in v1; plugins encode their constraints in `inputSchema`. Revisit if a real plugin needs richer pre-flight checks.\n- Free-text rejection messages: model-readable structured rejection lets the model fix the specific field it got wrong.\n- Validating *post*-action (verifying the action achieved its goal): that's the job of the next observation+diff, not the validator.\n\n## Non-obvious notes\n- Depends on .11 (typed VmServiceClient — actually unused at runtime here, but session owns merged tool list), .12 (typed observation shape), .3 (ExplorationTool's inputSchema). Blocks .18.\n- Per PRD §17, the **3-retry budget is per observation**, not per session. The validator is stateless; the retry counter lives on the loop driver (.18).\n- Plugin-owned identifier convention (PRD §21 open question, captured in §12.3): plugin observation fragments include the IDs accepted by their tools. The validator doesn't enforce this — it relies on the plugin's `inputSchema` to declare ID shape — but the model relies on it for valid choices.\n- Cross-references: PRD §10 step 7 (validation step), §12.1 (core tool list), §12.3 (action targeting), §17 (failure modes — invalid action retries).","acceptance_criteria":"- [ ] Returns ok | reject(reason) for any candidate action\n- [ ] Rejects unknown tool names, schema mismatches, missing/disabled/off-screen target nodes\n- [ ] Plugin-tool args validated against the plugin's declared schema\n- [ ] Rejection messages model-readable","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:18Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:50:12Z","dependencies":[{"issue_id":"lenny-cx6.17","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:17Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.17","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:26Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.17","depends_on_id":"lenny-cx6.12","type":"blocks","created_at":"2026-05-07T09:01:27Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.17","depends_on_id":"lenny-cx6.3","type":"blocks","created_at":"2026-05-07T09:01:27Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.15","title":"MLX model provider for Qwen3.6-35B-A3B","description":"OpenAI-compatible HTTP client targeting an MLX-served Qwen3.6-35B-A3B at 8-bit. Required behaviors: vision input (image_url base64), tool-call parser per the spike outcome, preserve_thinking=true by default for KV cache reuse and consistency, sampling defaults temperature=1.0 / top_p=0.95 / top_k=20 / presence_penalty=1.5 / repetition_penalty=1.0. CORS-safe for browser-based requests from the DevTools panel. Depends on the spike (D1) recommendation for tool-call parser handling. PRD §16.3, §22.","design":"## Approach\nImplement `MlxModelProvider` against an OpenAI-compatible HTTP endpoint serving Qwen3.6-35B-A3B at 8-bit on M3 Ultra.\n\n**Approach branches on the lenny-18f spike outcome.** The spike verifies whether `mlx-vlm` correctly handles the `qwen3_coder` tool-call parser format. Four possible outcomes:\n\n1. **`mlx-vlm-primary`** (best case): mlx-vlm supports `qwen3_coder` cleanly. Provider hits mlx-vlm directly with vision + tool-call requests; nothing exotic in the client.\n2. **`mlx-lm-text-fallback`**: tool-calling works on `mlx-lm` text-only mode but `mlx-vlm` is broken for tool calls. Provider drops to `mlx-lm`; capability metadata reports `vision=false`. Document the regression in the README.\n3. **`sglang-linux-fallback`**: neither MLX path works; user runs SGLang on Linux with `--tool-call-parser qwen3_coder`. Provider's HTTP shape is the same OpenAI-compatible interface; only the URL and operator setup change. We lose M3 Ultra colocation but keep the rest of the design.\n4. **`parser-shim`**: MLX returns tool calls in a non-conforming envelope; we ship a small client-side reshape that maps Qwen's emitted format to OpenAI tool-calls before handing to the schema validator.\n\nThe provider abstraction (.14) is the same in all four; only this concrete impl differs.\n\n**Required behaviors regardless of branch:**\n- **Vision input default-on.** When the observation includes a screenshot (PRD §11.1, §16.3), attach as `image_url` with base64-encoded PNG. Capability metadata reports `vision=true` except in branch 2.\n- **`preserve_thinking=true` by default.** Qwen3.6 explicitly recommends this for agent scenarios — improved KV cache reuse, decision consistency, less redundant reasoning (PRD §16.3).\n- **Sampling defaults**: `temperature=1.0, top_p=0.95, top_k=20, presence_penalty=1.5, repetition_penalty=1.0`. The high `presence_penalty` is Qwen's specifically-recommended anti-loop value; document that it's tuned for this model only.\n- **CORS-safe.** DevTools panel runs in-browser (PRD §6.3, §22), so the local mlx-vlm/sglang server must allow cross-origin requests from the panel's origin. Ship a sample server config (CORS headers documented in the README) per PRD §22 risk mitigation.\n- **Thinking stream**: parse `\u003cthink\u003e...\u003c/think\u003e` blocks from the streaming response and emit `ThinkingDelta` events for the DevTools thinking panel (.23).\n\n## Constraints\n- **Web-compatible** per PRD §22: HTTP only; no native SDKs. `package:http` is fine.\n- Vision payloads can be large (PNG screenshots). Don't accidentally `dart:io.File`-read; the screenshot arrives in-memory from the host's screenshot capture (.7) as bytes.\n- Tokenizer: for prompt-budget enforcement (.13's hard cap), need a token counter. If the server exposes one, use it; otherwise ship `package:tiktoken_dart` or a Qwen tokenizer port. Document the choice.\n\n## Ruled out\n- Direct MLX integration via FFI: redundant given OpenAI-compatible HTTP, and breaks web-compat.\n- Forcing `preserve_thinking=false` to \"save tokens\": Qwen's own guidance contradicts this, and the M3 Ultra has the cache headroom.\n- Tightening sampling (`temperature\u003c1.0`): Qwen specifically recommends 1.0 with the listed top_p/top_k/penalties for thinking mode. Tuning these without empirical data costs more than it saves.\n- Bundling a model ourselves: out of scope — user stands up the inference server, we just talk to it.\n\n## Ordering\n- **Blocked on lenny-18f** (the spike). Cannot start this story until the spike outcome is recorded; the impl path depends on it.\n- Depends on .14 (interface). Builds on .14's capability surface and dynamic schema.\n\n## Non-obvious notes\n- This is the v1 primary local-inference path. .16 (Anthropic + OpenAI) is the fallback when this stalls (PRD §16.4).\n- Cross-references: PRD §16.3 (full Qwen3.6 spec), §22 (CORS + harness-in-web-app constraints + the very mlx-vlm risk this story addresses), §11.1 (vision default-on logic).\n- After the spike resolves, capture the chosen branch in this bead's notes/comments so the build worker doesn't re-derive it.\n- The \"parser-shim\" branch is the riskiest impl-wise — if we end up there, validation surface in .17 may need a hook to accept the shim's output before mainstream schema check.","acceptance_criteria":"- [ ] OpenAI-compatible client works against mlx-vlm (or chosen fallback per D1)\n- [ ] Vision input attached when screenshot is included in observation\n- [ ] preserve_thinking=true by default\n- [ ] Sampling defaults applied per Qwen recommendations\n- [ ] Tool-call parser path matches D1 outcome\n- [ ] CORS headers documented; sample server config committed\n- [ ] Capability metadata reports vision=true, preserve_thinking=true","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:16Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:49:12Z","dependencies":[{"issue_id":"lenny-cx6.15","depends_on_id":"lenny-18f","type":"blocks","created_at":"2026-05-07T09:01:25Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.15","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:15Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.15","depends_on_id":"lenny-cx6.14","type":"blocks","created_at":"2026-05-07T09:01:24Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.14","title":"Model provider abstraction with dynamic constrained-output schema","description":"ModelProvider interface with completion + schema-constrained variants. Schema is dynamically composed each turn from the current merged tool list — different plugins yield different valid action shapes. Output shape: {action: {tool, args}, summary_update, rationale?, wait_strategy?}. Outputs that don't match the schema are rejected and re-prompted once. Surfaces model capabilities (vision, preserve_thinking, max context) so the host can default screenshot capture appropriately. PRD §16.","design":"## Approach\nDefine the `ModelProvider` interface and the dynamic schema composer that sits in front of every concrete provider.\n\n**Interface:**\n```dart\nabstract class ModelProvider {\n  ModelCapabilities get capabilities; // {vision, preserveThinking, maxContext, supportsToolUse}\n  Stream\u003cThinkingDelta\u003e thinking();   // For DevTools thinking panel (.23)\n  Future\u003cModelDecision\u003e decide(PromptPayload prompt, ActionSchema schema);\n}\n```\n\n`ModelDecision` is the v0.5 output shape: `{action: {tool, args}, summary_update, rationale?, wait_strategy?}`.\n\n**Dynamic schema composer.** `ActionSchema.fromToolList(mergedTools) → JsonSchema`. **Composed per turn** from the current merged tool list (PRD §16.2): the `action` field becomes a discriminated union over each available tool's name + that tool's `inputSchema`. Different plugins → different schema shape → genuinely different valid action set. The composer is called by the loop driver (.18) on every turn just before `decide()`.\n\n**Schema-rejected output handling.** If a provider returns output that fails schema validation, surface a typed `SchemaRejection` with the validation error attached. The loop driver retries once with the error injected into the prompt (PRD §17 \"Malformed model output. One retry with schema error\"); a second failure counts as a failed turn.\n\n**Capabilities surface.** `vision` (defaults screenshot capture on per PRD §11.1, §16.3), `preserveThinking` (Qwen3.6 only — concrete providers in .15/.16 set this), `maxContext`, `supportsToolUse`. The session reads these at start to set defaults the user can override.\n\n## Constraints\n- **Web-compatible** per PRD §22: HTTP-only providers acceptable; no SDKs that pull in `dart:io`. .15 and .16 must respect this when picking SDKs.\n- Schema must be valid JSON Schema draft-07 or later (what most providers' constrained-output features accept).\n- Streaming thinking output is required for the DevTools thinking panel (.23) — the interface must support it from day one even if some providers won't populate it.\n\n## Ruled out\n- Statically composed action schema (compiled once at session start): contradicts PRD §16.2 and breaks plugin auto-disable mid-session — the schema would still admit a disabled plugin's tools.\n- A single mega-tool with a `tool_name: string` + free-form args: loses constrained-output's strongest property (the model can only produce tool calls that exist with valid args). Discriminated union is the whole point.\n- Tool-aware retry beyond one (re-prompting with the schema error): per PRD §17, more than one retry per malformed output blurs the failed-turn budget and lets a stuck model burn the session budget on parse errors.\n- Provider-specific capability flags as opaque maps: typed `ModelCapabilities` keeps the call sites in .15/.16/.18 honest.\n\n## Non-obvious notes\n- Depends on .11 (provider owns no VM state but lives in the harness lib). Blocks .15 (MLX) and .16 (Anthropic+OpenAI), which are concrete impls.\n- The dynamic-schema-per-turn requirement is the same load-bearing v0.5 invariant noted on .13. Test it by composing a schema with a mock 2-plugin set, then with a 1-plugin set, and asserting the JSON differs.\n- Capability-driven defaults (vision → screenshots on) are detected by .18; this story just exposes the metadata shape.\n- Cross-references: PRD §16.1 (interface), §16.2 (dynamic constrained output), §16.3 (Qwen3.6 capabilities), §17 (failure modes for malformed output).","acceptance_criteria":"- [ ] ModelProvider interface compiles and supports schema-constrained calls\n- [ ] Dynamic schema composition produces valid JSON Schema for the merged tool list each turn\n- [ ] Schema rejection retries once before counting a failed turn\n- [ ] Capability metadata (vision, preserve_thinking) surfaced for host defaults","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:15Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:36Z","dependencies":[{"issue_id":"lenny-cx6.14","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:14Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.14","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:23Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.12","title":"Observation assembly \u0026 per-turn structural diff (core + plugins)","description":"Pull observation bundle via getStableObservation, normalize, compute per-turn structural diff for both core fragment and each plugin fragment. Plugins owning a delta-friendly shape get a structured diff; opaque blobs fall back to a previous-vs-current pair. Diffs are harness-authored and included verbatim in the next prompt. PRD §10, §11.3.","design":"## Approach\nTwo cooperating modules inside `exploration_agent`:\n\n1. **Observation puller.** Single VM call to `ext.flutter.exploration.getStableObservation(policy)` returns the merged bundle the host already assembled (.8 owns assembly host-side). The harness deserializes into typed Dart records: `Observation { core: CoreFragment, plugins: Map\u003cString, Object?\u003e, stability: StabilityMetadata }`.\n2. **Diff engine.** `ObservationDiffer.diff(prev, curr) → ObservationDiff`. Computes:\n   - **Core diff** (structural, well-known shape): `routeChanges`, `nodesAdded`, `nodesRemoved`, `nodesChanged` (semantics tree), `errorsAdded`. Stable per PRD §11.1.\n   - **Per-plugin diffs**: dispatched by namespace. For plugins whose fragment is a JSON-shaped map of named keys (the convention plugins should follow per PRD §7.3), compute a structured key-level delta. For opaque blobs, fall back to `{previous, current}` pair per PRD §11.3.\n   - **First turn**: `prev` is `Observation.empty()`, treated as having no nodes/no plugin keys; everything is an \"add\". No null-pun crashes.\n\nDiff result is harness-authored (mechanical), goes verbatim into the next prompt (consumed by .13), and into the trajectory record (.19).\n\n## Constraints\n- **Web-compatible** per PRD §22: pure Dart, no `dart:io`. JSON-only in/out.\n- Diff must be deterministic — same input pair always yields the same diff structure (important for trajectory readability).\n- Plugin diff dispatch must be tolerant: an unknown plugin namespace in `curr` (e.g., a plugin that just registered) is a fresh \"added\" fragment; one that disappeared from `curr` (auto-disabled by .18) is a clean removal, not a crash.\n\n## Ruled out\n- Letting plugins compute their own diff via the contract: explicitly host-authored per PRD §11.3, and adding a contract method would expand the contract surface unnecessarily for v1.\n- Streaming partial observation: the host already commits to an atomic snapshot in .8; partial observations would defeat the stability primitive.\n- Deep semantic equality on whole trees: shallow ID-keyed delta is enough for the prompt budget and avoids quadratic blowup on large semantics trees.\n\n## Non-obvious notes\n- Depends on .11 (VmServiceClient) and .8 (host-side assembly). Pulling is a one-liner against the client; the bulk is the differ.\n- Blocks .13 (prompt assembly consumes the diff), .17 (validator wants the typed observation), and indirectly .18 (consumes both via .13 and .17).\n- The \"delta-friendly shape\" criterion is a per-plugin convention — at registration, a plugin can opt into structured diffing by declaring the keys are stable; otherwise opaque-blob fallback. Document this hook in the plugin guide (.35).\n- Cross-reference: PRD §10 step 4 (diff is its own loop step), §11.3 (diff covers both), §11.4 (budget = serialized observation; diff is unbudgeted but informally bounded).","acceptance_criteria":"- [ ] Observation pulled in single VM call, returned as typed bundle\n- [ ] Core diff: routeChanges, nodesAdded, nodesRemoved, nodesChanged\n- [ ] Per-plugin structured diff when shape is delta-friendly\n- [ ] Opaque-blob plugins get previous-vs-current fallback\n- [ ] First-turn diff treats prior observation as empty without crashing","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:13Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:47:40Z","dependencies":[{"issue_id":"lenny-cx6.12","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:13Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.12","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:21Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.12","depends_on_id":"lenny-cx6.8","type":"blocks","created_at":"2026-05-07T09:01:20Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.11","title":"Harness scaffolding, VM service connection, and ExplorationSession API","description":"exploration_agent library shell that connects to a running app's VM service URI, performs the binding handshake, and exposes a public ExplorationSession API the two frontends consume. Web-compatible by construction (no dart:io anywhere in this package). Provides typed VmServiceClient abstraction reused by later harness stories. PRD §6.2.","design":"## Approach\nStand up `exploration_agent` as a pure Dart library (no executable entrypoint) whose public surface is the `ExplorationSession` class. v0.5 refactored the harness/frontend boundary so this library is consumed by both `exploration_cli` (CLI frontend) and `exploration_devtools` (DevTools extension panel) — same loop, two surfaces, no reimplementation.\n\n`ExplorationSession` owns the lifecycle: `start(goal, config)`, `observe()`, `act(action)`, `end()`, plus a stream-based progress channel for the DevTools thinking panel. Internally it composes a typed `VmServiceClient` abstraction that wraps `package:vm_service` for the calls the harness needs (binding handshake, `getStableObservation`, `executeAction`, namespaced extension dispatch). This client is the shared seam reused by .12 (observation pull), .14 (model provider state), .17 (validator), and .19 (writer).\n\nHandshake protocol: connect to VM service URI, look up `ext.flutter.exploration.handshake`, exchange contract-version + active-plugin manifest. Absence of the binding extension → typed `BindingNotInitializedError` whose message points at the host setup snippet from PRD §7.6.\n\n## Constraints\n- **MUST be web-compatible** per PRD §22: no `dart:io`, no transitive imports of it. CI guard verifies the package's transitive dep graph contains no `dart:io` symbols. Trajectory I/O is delegated to .19's writer abstraction; the harness library itself never touches the filesystem directly.\n- Public API is the only commitment surface — internals are private and rearrangeable in later stories.\n- Single binding per process (the host enforces this in .2); we can assume exactly one ExplorationBinding on the other end.\n\n## Ruled out\n- Harness as a CLI executable (v0.4 shape): broken by v0.5 because two frontends now consume the same loop. A library is the only shape that supports both without IPC duplication.\n- Running the harness as a separate process from DevTools and IPC-ing into the panel: PRD §6.3 requires the loop run inside the extension web app itself; that forecloses any process-boundary design.\n- Hand-rolled VM service JSON-RPC: package:vm_service is the supported Dart abstraction.\n\n## Non-obvious notes\n- Blocks .12, .13, .14, .17, .19, .21, .25, .26, .27 — this is the harness anchor. Land its public API first; later stories implement against the surface.\n- The `VmServiceClient` typed abstraction is load-bearing across phase 2; spending design weight on its shape here saves churn in .12/.17/.19.\n- Cross-references: PRD §6.2 (library refactor), §6.3 (in-panel loop requirement), §22 (web-compat constraint), §7.6 (binding handshake message shape).\n- Plugin auto-disable state lives on the session (consumed by .18); expose a minimal mutator now so .18 doesn't need to reach into private state.","acceptance_criteria":"- [ ] ExplorationSession is the public API and supports start/observe/act/end lifecycle\n- [ ] Connects via VM service URI and runs handshake against ExplorationBinding\n- [ ] Fails with setup-pointing error if binding is absent\n- [ ] Package has no transitive dart:io import (verified in CI)\n- [ ] VmServiceClient typed abstraction shared across harness modules","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:12Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:47:19Z","dependencies":[{"issue_id":"lenny-cx6.11","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:11Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.11","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:08Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.11","depends_on_id":"lenny-cx6.2","type":"blocks","created_at":"2026-05-07T09:01:19Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.11","depends_on_id":"lenny-cx6.3","type":"blocks","created_at":"2026-05-07T09:01:20Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":3,"dependent_count":9,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.8","title":"Stable observation primitive (getStableObservation)","description":"Binding extension that polls framework signals and busyState() from all plugins until the chosen policy terminates, then captures and returns the merged observation: core fragment (semantics, routes-best-effort, errors, stability metadata, optional screenshot) + per-plugin fragments under observation.plugins.\u003cnamespace\u003e. Implements three policies: action-relative (default 800ms budget), quiet-frame (default N=2), bounded-stability (default 1500ms budget). Per-plugin observation budget enforcement (default 1KB serialized) with warning + truncation on overrun. Stability metadata reports terminated_by, duration_ms, framework_busy, plugins_busy[]. PRD §9, §10, §11.","design":"## Approach\nImplement the host's `getStableObservation(policy)` extension on `ExplorationBinding`, exposed as a single VM service call `ext.flutter.exploration.core.get_stable_observation`. The implementation polls the framework signal snapshot from cx6.4 and `busyState()` from every registered plugin (in registration order, with per-call try/catch from cx6.3) until the chosen policy terminates, then captures and serializes the merged observation. Three policies (PRD §9.1):\n\n- **action-relative (default).** Wait until any of: route stack changes, semantics tree changes, no plugin reports busy, or 800ms wall-clock.\n- **quiet-frame.** Wait until N consecutive committed frames with no transient/persistent callbacks, no pending microtasks, and no plugin busy (default N=2).\n- **bounded-stability.** Wait until quiet-frame achieved or 1500ms wall-clock; on budget expiration capture anyway and tag with what's still busy.\n\nComposition: core fragment (semantics from cx6.5, best-effort Navigator 1.0 routes, errors from cx6.9, stability metadata, optional screenshot from cx6.7) + per-plugin fragments under `observation.plugins.\u003cnamespace\u003e` from cx6.3's `observe()` calls.\n\n**Budget split (PRD §11.4):** total observation budget defaults to 6KB serialized (~2K tokens). Distributed as: **core 4KB**, **plugins 2KB total** distributed by configured per-plugin budgets (default 1KB each, overridable via session config). Per-plugin truncation emits a warning and includes a truncation marker in the fragment; core overrun also warns. Stability metadata reports `policy`, `terminated_by`, `duration_ms`, `framework_busy`, `plugins_busy[]`.\n\n## Constraints\n- Single VM call returns the full merged observation bundle — the harness is web-running and we don't want N round-trips per turn.\n- Replaces `pumpAndSettle` (PRD §9): real apps with live network/animation/timers never reach a `pumpAndSettle` fixed point, so the policies are explicit time-bounded compositions of framework + plugin signals.\n- Plugin failures during `busyState()` are isolated per cx6.3's contract — a throwing plugin contributes \"not busy\" for that turn and counts toward its 3-strikes auto-disable.\n- Web-compatible serialization: pure JSON, no Dart-specific types in the wire format.\n- kDebugMode gating (PRD §6.1).\n\n## Ruled out\n- A single global \"wait until idle\" policy: rejected — different turns want different stability semantics. Action-relative for normal turns, quiet-frame for first observation of a session, bounded-stability for the honest middle ground (PRD §9.1).\n- Capturing observations only when fully stable: rejected — bounded-stability deliberately captures-anyway and tags, because that's more honest than blocking forever (PRD §9.1).\n- Per-plugin observations as separate VM calls: rejected — bundling into a single call keeps the harness simple and avoids partial-observation races.\n\n## Non-obvious notes\n- The 800ms default for action-relative is empirically tunable; cx6.30 (deferred 45d) tracks the empirical task. Do not hardcode — accept overrides via session config.\n- The model can request a specific policy for its next observation via a `wait_strategy` field on its action (PRD §9.1 last paragraph, §16.2). This bead exposes the policy selector; cx6.18's loop driver wires the model's request through.\n- Plugin observation budget enforcement happens at serialization time: serialize each plugin fragment, measure byte size, truncate if over budget, emit a warning. Truncation MUST leave a structurally valid marker so the model knows the fragment was clipped.\n- Stability metadata's `plugins_busy[]` includes `{namespace, reason, est_ms}` per PRD §9.2 — the model uses this to decide whether to wait or proceed.\n- This bead converges five upstream prerequisites (cx6.3, .4, .5, .7, .9) and is itself the convergence point cx6.12 builds on for diff computation.","acceptance_criteria":"- [ ] action-relative terminates on route/semantics change OR all plugins idle OR 800ms\n- [ ] quiet-frame terminates after N consecutive idle frames including plugin idle\n- [ ] bounded-stability terminates on quiet-frame OR 1500ms; honest tagging when budget hit\n- [ ] Total observation budget defaults to 6KB serialized (~2K tokens) per PRD §11.4\n- [ ] Core fragment budget defaults to 4KB; plugins share 2KB total distributed by per-plugin budgets\n- [ ] Per-plugin observation budget defaults to 1KB, overridable via session config; truncation emits a warning\n- [ ] Returned stability block includes framework_busy and plugins_busy[]\n- [ ] Single VM call returns the full merged observation bundle","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:10Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:50:15Z","dependencies":[{"issue_id":"lenny-cx6.8","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:09Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.8","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:06Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.8","depends_on_id":"lenny-cx6.3","type":"blocks","created_at":"2026-05-07T09:01:15Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.8","depends_on_id":"lenny-cx6.4","type":"blocks","created_at":"2026-05-07T09:01:15Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.8","depends_on_id":"lenny-cx6.5","type":"blocks","created_at":"2026-05-07T09:01:16Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.8","depends_on_id":"lenny-cx6.7","type":"blocks","created_at":"2026-05-07T09:01:17Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.8","depends_on_id":"lenny-cx6.9","type":"blocks","created_at":"2026-05-07T09:01:18Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":6,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.6","title":"Core action tools (core namespace)","description":"Implement and register the host's 10 core tools under the 'core' namespace: core.tap, core.long_press, core.enter_text, core.scroll, core.scroll_until_visible, core.gesture (discrete kinds), core.system_back, core.wait (rejected if \u003e5s), core.inspect_widget, core.done. All target semantics node IDs except wait/done. PRD §12.1.","design":"## Approach\nImplement the host's 10 core action tools and register them under the reserved `core` namespace via the registry from cx6.3. Each tool is an `ExplorationTool` with a typed JSON-schema input and an executor that resolves a target semantics node ID (from cx6.5) into the right Flutter operation: `core.tap`, `core.long_press`, `core.enter_text`, `core.scroll`, `core.scroll_until_visible`, `core.gesture` (discrete kinds only — pan/swipe/pinch with bounded parameters), `core.system_back`, `core.wait` (rejected if \u003e5s), `core.inspect_widget` (returns the widget subtree for a target), `core.done` (terminal). Tools dispatch through a `SemanticsAction` send where applicable; for tap/scroll on widgets that don't accept semantics actions, fall back to a hit-test at the node's center. `enter_text` focuses the target before typing.\n\n## Constraints\n- All targeting goes through semantics node IDs from cx6.5 — never coordinates (PRD §12.3). The agent reasons over IDs; coordinates are an internal implementation detail.\n- `core` namespace is reserved by the host; the registry from cx6.3 must reject any plugin attempting to register tools under `core`.\n- `core.wait` MUST reject values \u003e5s — the budget belongs to the harness loop driver (cx6.18), not to a model that can write `wait(seconds: 60)`.\n- `core.scroll_until_visible` MUST terminate with a clear error if the target isn't reachable (avoid infinite scrolling); cap iterations.\n- All tools registered as VM service extensions under `ext.flutter.exploration.core.\u003ctool\u003e`.\n\n## Ruled out\n- Coordinate-based targeting: rejected (PRD §12.3 — semantics IDs are the action surface).\n- Continuous / arbitrary-path gestures: out of v1 (PRD §18 \"Custom gestures beyond the discrete kinds in §12.1\"). `core.gesture` accepts only the discrete kinds enumerated in §12.1.\n- Allowing `core.wait` for indefinite waits: rejected because waiting is the stability primitive's job (cx6.8), not the model's. The 5s cap turns wait into a small nudge for known-slow operations.\n- Combining tap+enter_text into a single composite tool: rejected — explicit, single-effect tools give the model better trajectories and easier validation (cx6.17).\n\n## Non-obvious notes\n- Validation in cx6.17 will check that the target node ID exists in the current observation — this bead doesn't need to redo that, but the executor SHOULD fail gracefully if the node has been disposed between observation and action (a real race in live apps).\n- `core.inspect_widget` is the agent's introspection escape hatch when the semantics tree is sparse; the returned subtree should be capped in size.\n- `core.done` emits a structured terminal record (with the agent's stated reason); the loop driver in cx6.18 reads this to know the session is finished.\n- This bead is a hard prerequisite for cx6.18 (loop driver dispatches actions). Test each tool against an instrumented app fixture; PRD success criterion 2 (login → settings → toggle → logout) exercises tap/enter_text/scroll heavily.","acceptance_criteria":"- [ ] All 10 core tools execute against an instrumented app\n- [ ] Tools target semantics node IDs, not coordinates\n- [ ] enter_text correctly focuses target before typing\n- [ ] scroll_until_visible terminates with a clear error if unreachable\n- [ ] inspect_widget returns the widget subtree for the target\n- [ ] wait rejects values \u003e5s; done emits the requested reason","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:08Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:49:19Z","dependencies":[{"issue_id":"lenny-cx6.6","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:07Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.6","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:04Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.6","depends_on_id":"lenny-cx6.5","type":"blocks","created_at":"2026-05-07T09:01:13Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.5","title":"Semantics tree capture with stable IDs","description":"Walk the semantics tree, assign session-stable numeric IDs, filter off-screen / fully-obscured / excludeSemantics nodes, and emit role/label/state-flags/available-actions per node. Expose as a host VM service extension. Apps with custom design systems whose controls don't expose semantics natively are addressed at the plugin layer (PRD §6.1: the host explicitly does not own custom-design-system knobs in v0.5; that surface is a plugin concern). PRD §11.1, §12.3.","design":"## Approach\nWalk Flutter's semantics tree from the root `SemanticsOwner` and emit a compact serializable list of node records: `{id, role, label, state_flags, available_actions, bounding_rect}`. Assign session-stable numeric IDs by maintaining a `Map\u003cSemanticsNode, int\u003e` (or `Map\u003cint /* SemanticsNode.id */, int /* session-stable */\u003e`) so that the same node carries the same ID across observations within a session. Filter out off-screen, fully obscured, and `excludeSemantics` nodes by default. Expose the capture as a host VM service extension `ext.flutter.exploration.core.get_semantics` so cx6.8 can compose it into the core observation fragment and cx6.6's core action tools can target node IDs.\n\n## Constraints\n- Stable IDs must persist across observations within a session (PRD §11.1, §12.3) — they are the targeting surface for `core.tap` etc. in cx6.6.\n- Output must fit in the 4KB core fragment budget (PRD §11.4) for typical screens. Keep records compact: short field names, omit empty/default values, avoid duplicating bounding rects when not needed.\n- Web-reachable: the harness (web target via DevTools) reads this over VM service; the serialized form must be plain JSON.\n- kDebugMode gating (PRD §6.1).\n\n## Ruled out\n- The v0.2 `isInteractiveWidget` host hook is OUT in v0.5. Per PRD §6.1: \"The host does not own ... anything about specific custom design systems beyond the configuration knobs Marionette also exposes (which can themselves be a plugin in this design).\" Custom-design-system widgets that don't expose semantics natively are addressed by writing a plugin, not by configuring the host. This is one of the v0.5 corrections vs v0.2's host-centric posture.\n- Including off-screen nodes by default: rejected — semantics tree is the agent's action surface, and acting on something the user can't see is a known failure mode. Off-screen nodes can be requested via cx6.6's `core.scroll_until_visible` tool.\n- Returning the raw `SemanticsData` blob: rejected — it includes Flutter-internal fields the agent doesn't need and bloats the budget.\n\n## Non-obvious notes\n- \"Apps with custom design systems whose controls don't expose semantics natively\" is the failure case cx6.10's diagnostic warns about and the plugin authoring guide (cx6.35) tells users how to fix. v0.5's answer is unambiguously \"write a plugin\"; the host warns and points at the guide rather than offering a knob.\n- ID stability is per-session, not cross-session. Trajectory replay (v2, §15.4) will need a different identity story; do not over-engineer for it here.\n- This bead is a hard prerequisite for cx6.6 (core tools target node IDs), cx6.8 (core observation fragment), and cx6.10 (diagnostic walks the same tree to find missing semantics). Keep the walk reusable.\n- Bounding rects are useful for screenshot disambiguation in vision-model flows (PRD §11.1, §16.3); keep them in the record but consider quantizing to integer pixels to save bytes.","acceptance_criteria":"- [ ] Semantics tree serialized as compact node records (id, role, label, state, actions)\n- [ ] Off-screen / excluded / obscured nodes omitted by default\n- [ ] Stable IDs persist across observations within a session\n- [ ] VM extension returns the captured tree","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:07Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:49Z","dependencies":[{"issue_id":"lenny-cx6.5","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:07Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.5","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:04Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.5","depends_on_id":"lenny-cx6.2","type":"blocks","created_at":"2026-05-07T09:01:12Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.4","title":"Frame stability signal collection (host)","description":"Hook SchedulerBinding to track transient callbacks (animations), persistent callbacks, pending microtasks, frame commit/skip events. Maintain bounded counters and timestamps suitable for stability decisions. Used by getStableObservation. PRD §6.1, §9.","design":"## Approach\nHook `SchedulerBinding` from inside `ExplorationBinding` to maintain a live snapshot of framework-level busy signals: count of in-flight transient callbacks (animations, ticker driven work), persistent callbacks, presence of pending microtasks, and frame commit/skip events with timestamps. Maintain bounded counters (no unbounded history) and expose a synchronous `frameworkBusySnapshot()` method returning a small immutable record (`{transientCallbacks, persistentCallbacks, pendingMicrotasks, lastFrameCommitTimestamp, recentSkippedFrames}`) suitable for both the stability policy loop in cx6.8 and the stability-metadata block emitted in observations.\n\n## Constraints\n- Zero allocation on the hot path where possible — this is polled by `getStableObservation` (cx6.8) and may be called many times per turn.\n- Counters MUST be bounded; never grow with session length.\n- Microtask presence is a \"best-effort observation\" — Dart does not give us a public count API, so we observe via scheduling-edge heuristics. Document the limitation; cx6.8's policy treats microtasks as one signal among several.\n- kDebugMode-gated like the rest of the host (PRD §6.1).\n\n## Ruled out\n- Reusing `pumpAndSettle` semantics: explicitly rejected throughout v0.2/v0.3/v0.4/v0.5 (PRD §9 \"This replaces `pumpAndSettle` for the same reasons as v0.2 (real apps never settle in the test-framework sense)\"). Real apps with live network and real time never reach a `pumpAndSettle` fixed point.\n- Globally tracking every microtask: not feasible from public Dart APIs and would have prohibitive overhead. We track presence/edge signals, not counts.\n- Including plugin-contributed busy-state here: that lives in cx6.8's policy loop, which composes this framework snapshot with `busyState()` calls per PRD §7.4 and §9.1.\n\n## Non-obvious notes\n- The exact API shape is consumed by cx6.8's policy loop and cx6.10's diagnostic; design `frameworkBusySnapshot()` to be stable.\n- The \"framework_busy\" block reported in observation stability metadata (PRD §9.2) maps directly onto this snapshot — keep the field names aligned so cx6.8 doesn't need translation logic.\n- Quiet-frame policy needs frame-commit timestamps to count \"N consecutive frames\" (PRD §9.1); `lastFrameCommitTimestamp` plus a small ring of recent-commit timestamps is what enables that.\n- Action-relative policy needs a fast \"any framework signal busy?\" check; expose a derived getter that returns true if any of transient/persistent/microtask is non-zero/present.","acceptance_criteria":"- [ ] In-flight transient and persistent callback counts observable\n- [ ] Pending microtask presence observable\n- [ ] Frame commit/skip events timestamped\n- [ ] Snapshot exposed for stability evaluation","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:06Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:23Z","dependencies":[{"issue_id":"lenny-cx6.4","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:06Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.4","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:03Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.4","depends_on_id":"lenny-cx6.2","type":"blocks","created_at":"2026-05-07T09:01:11Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.3","title":"Plugin contract: types, registry, and PluginContext","description":"The load-bearing v0.5 design artifact. Define ExplorationPlugin, ExplorationTool, BusyState, ObservationContext, ExecutedAction, ToolResult, JsonSchema. Implement the plugin registry: registration order preserved, namespace uniqueness enforced (^[a-z][a-z0-9_]*$), tool names mandatorily prefixed (\u003cnamespace\u003e.\u003ctool\u003e), collision rejection at startup. Implement PluginContext with registerErrorHandler, registerExtension (namespaced under ext.flutter.exploration.\u003cnamespace\u003e.\u003csuffix\u003e), registerFrameCallback. Lifecycle: initialize() in registration order, dispose() at session end. Per-method exception isolation; auto-disable a plugin after 3 consecutive failures. PRD §7.","design":"## Approach\nImplement the load-bearing v0.5 design artifact: the typed Dart plugin contract (PRD §7.1–§7.8). Define the public types in `exploration_flutter/lib/src/contract/`: `ExplorationPlugin` (interface with `namespace`, `tools`, `initialize`, `observe`, `busyState`, `onActionExecuted`, `dispose`), `ExplorationTool` (`name`, `description`, `inputSchema`, `call`), `BusyState` (`isBusy`, `reason?`, `estimatedDuration?`), `ObservationContext`, `ExecutedAction`, `ToolResult`, and a thin `JsonSchema` wrapper. Build the registry inside the binding: validates `namespace` against `^[a-z][a-z0-9_]*$`, enforces uniqueness, mandatorily prefixes every tool name as `\u003cnamespace\u003e.\u003ctool\u003e`, and rejects collisions at startup. Implement `PluginContext` exposing `registerErrorHandler(ErrorHandler)`, `registerExtension(suffix, handler)` (auto-namespaced under `ext.flutter.exploration.\u003cplugin.namespace\u003e.\u003csuffix\u003e`), and `registerFrameCallback(FrameCallback)`. Lifecycle: `initialize()` runs in registration order; per-method exception isolation wraps every contract call in try/catch; a per-plugin failure counter auto-disables the plugin after 3 consecutive failures with a clear log entry.\n\n## Constraints\n- This is the load-bearing v0.5 artifact — per the v0.5 changelog (\"Restructured around a host-and-plugins architecture\") the entire design depends on this contract being right. Treat the public types as if they were stable from day one even though we reserve the right to break them (PRD §22 plugin-contract-churn risk; mark v1 contract experimental).\n- PRD §7.7 versioning posture: plugin updates that add tools, expand observation fragments, or refine busy-state heuristics are NOT breaking changes. The host MUST treat unfamiliar fragment fields as opaque pass-through; new tools auto-appear in the merged tool list. Breaking changes are reserved for changes to the `ExplorationPlugin` interface itself, which the host versions explicitly. This is a deliberate design decision mirroring the Packaged AI Assets posture.\n- Contract must leave room for v2 resources/prompts (PRD §8): the `ExplorationPlugin` interface should be extendable in a backward-compatible way to add `List\u003cExplorationResource\u003e` and `List\u003cExplorationPrompt\u003e` getters; the `extension/exploration/config.yaml` manifest shape (§7.6) must accept future `resources:` and `prompts:` keys.\n- All types must be web-compatible at the seam exposed to the harness — the harness consumes the namespacing/schema via VM service, not by importing this package directly.\n\n## Ruled out\n- Auto-discovery of plugins from `pubspec.yaml`: out of v1 (PRD §18, §7.6). The convention `extension/exploration/config.yaml` is documented but not read by the host yet.\n- Plugin-to-plugin hard dependencies (e.g. `exploration_dio` requires `exploration_logging`): deferred (PRD §21 open question; default position \"no, plugins should be independent\").\n- A separate error-chaining design bead: replaced by `PluginContext.registerErrorHandler` here. The v0.2-era idea of a standalone error-chaining mechanism collapses into this contract surface; cx6.9's ring buffer plugs into the same hook.\n- Subclassing the binding as a plugin extensibility mechanism: rejected (PRD §7.5). Plugins request framework hooks via `PluginContext`; only the host owns the binding.\n\n## Non-obvious notes\n- This bead's API shape is consumed verbatim by cx6.8 (busyState polling, observe), cx6.9 (registerErrorHandler), cx6.17 (action validator reads merged tool list), cx6.25/.26/.27 (reference plugins implementing the contract), and cx6.35 (plugin authoring guide). Get the names and shapes right before downstream work starts.\n- The `ext.flutter.exploration.\u003cnamespace\u003e.\u003csuffix\u003e` auto-namespacing in `registerExtension` is what makes plugin VM extensions discoverable to the harness without the harness knowing about specific plugins (PRD §7.5).\n- Registration order is preserved across `observe()`, `busyState()`, `onActionExecuted()`, `dispose()` (PRD §7.8). Use a `List`, not a `Map`, as the registry's primary store.\n- The 3-strikes auto-disable rule applies independently to `observe()`, `busyState()`, and `onActionExecuted()`; PRD §7.8 is explicit about per-method isolation.\n- `initialize()` failures are loud but non-fatal — the plugin is marked failed at startup and the session continues without it (PRD §17).","acceptance_criteria":"- [ ] Plugin contract types compile and have stable API\n- [ ] Registry rejects duplicate namespaces and tool-name collisions at startup\n- [ ] PluginContext API works: error handlers chain, extensions register under correct namespace, frame callbacks fire\n- [ ] Plugin exceptions in any method don't kill the session; logged and isolated per-call\n- [ ] 3 consecutive failures auto-disable a plugin with a clear log entry\n- [ ] Versioning posture per PRD §7.7: plugin updates that add tools, expand observation fragments, or refine busy-state heuristics are NOT breaking changes; host treats unfamiliar fragment fields as opaque pass-through; new tools auto-appear in the merged tool list\n- [ ] Contract-level breaking changes (the ExplorationPlugin interface itself) are explicitly versioned by the host\n- [ ] Contract leaves room (in interface and config.yaml shape) for v2 resources/prompts (PRD §8)","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:05Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:01Z","dependencies":[{"issue_id":"lenny-cx6.3","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:05Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.3","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:02Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.3","depends_on_id":"lenny-cx6.2","type":"blocks","created_at":"2026-05-07T09:01:11Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":8,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.2","title":"ExplorationBinding skeleton, lifecycle, and conflict detection","description":"Host binding extends WidgetsFlutterBinding. ExplorationBinding.ensureInitialized(plugins: [...]) called from main(), kDebugMode/profile gating, hard error if another custom binding (IntegrationTestWidgetsFlutterBinding etc.) is already installed, registers the 'ext.flutter.exploration.*' core namespace and a handshake/ping extension. Defines the VM service extension naming convention used by core and namespaced for plugins. PRD §6.1.","design":"## Approach\nImplement `ExplorationBinding extends WidgetsFlutterBinding` in `exploration_flutter`, with a static `ensureInitialized({required List\u003cExplorationPlugin\u003e plugins})` entrypoint called from the user's `main()`. The binding installs itself as the singleton `WidgetsBinding`, performs a hard conflict check against any pre-existing custom binding (notably `IntegrationTestWidgetsFlutterBinding`), and registers the reserved `ext.flutter.exploration.*` VM service extension namespace plus a handshake / ping extension the harness uses to confirm the binding is live and report version metadata. This slice does NOT yet wire up the plugin registry implementation (that is cx6.3's deliverable) — it defines the seam: the constructor accepts the list and stores it for the registry to consume.\n\n## Constraints\n- `kDebugMode || kProfileMode` gating: outside those modes the binding is a no-op (PRD §6.1, §3 \"Non-goals: Running against a release-mode app\").\n- Only one binding per process — fail fast and loudly if another binding is already installed (PRD §6.1, §6.5: integration_test conflict).\n- VM service extension naming convention is reserved here: `ext.flutter.exploration.\u003ccore_or_plugin_namespace\u003e.\u003csuffix\u003e`. Document it because every downstream story (core tools, plugin extensions, screenshot, semantics) registers into this surface.\n- Web target: the binding itself is Flutter-side, but the extension naming and handshake contract must be reachable from the web-compatible harness (cx6.11) over VM service.\n\n## Ruled out\n- Subclassing `IntegrationTestWidgetsFlutterBinding` to \"compose\" with it: rejected. Two bindings cannot coexist (PRD §6.5, §20.1 Coexistence). Replay under integration_test is a v2 story (§15.4).\n- Auto-installing in release mode with a stripped surface: rejected — PRD §3 explicitly out of scope, and debug-mode VM service is the entire substrate.\n- Hosting plugin-registry logic inside this bead: deferred to cx6.3 to keep the binding skeleton minimal and conceptually small (PRD §6.1 \"small enough that its conceptual surface fits in a single document\").\n\n## Non-obvious notes\n- The handshake extension is what cx6.17 (binding-not-initialized error) and cx6.11 (harness connect) latch onto for clear error messages on misconfigured apps (PRD §17 \"Binding not initialized\").\n- Registration order of plugins matters (PRD §7.8). Persist the user-supplied list verbatim; cx6.3 reads it.\n- The binding intentionally hosts NO domain logic — it's the seam everything else attaches to. Resist the urge to inline frame stability (cx6.4), semantics (cx6.5), screenshots (cx6.7), or error capture (cx6.9) here.\n- This bead blocks cx6.3, .4, .5, .7, and indirectly cx6.11. Keep the surface tight.","acceptance_criteria":"- [ ] ExplorationBinding installs as the WidgetsBinding when initialized in main()\n- [ ] Throws a clear error if another binding has already been installed\n- [ ] No-op outside debug/profile mode\n- [ ] Registers 'ext.flutter.exploration.*' namespace and a handshake extension\n- [ ] VM service extension naming convention documented","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:04Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:47:23Z","dependencies":[{"issue_id":"lenny-cx6.2","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:04Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.2","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:01Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":1,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.1","title":"Repo \u0026 multi-package scaffolding","description":"Lay down the v1 package layout and CI baseline. Packages: exploration_flutter (host binding), exploration_agent (harness library, web-compatible — NO dart:io), exploration_cli (CLI frontend, where dart:io may live), exploration_devtools (DevTools extension, Flutter web app), exploration_router (reference plugin), exploration_riverpod (reference plugin), exploration_dio (reference plugin). Shared lints/analysis_options, baseline CI (analyze + test) across the workspace. Blocks every downstream story.","design":"## Approach\nLay down the v1 multi-package workspace as the foundation for every downstream story. Create a Dart workspace (or melos-managed monorepo) with seven packages: `exploration_flutter` (host binding, Flutter-targeting), `exploration_agent` (harness library, pure Dart, web-compatible), `exploration_cli` (Dart CLI with `dart:io`), `exploration_devtools` (Flutter web app built on `package:devtools_extensions`), and three reference plugin packages (`exploration_router`, `exploration_riverpod`, `exploration_dio`). Ship a shared `analysis_options.yaml` (or root lint package) referenced by every package. Stand up a baseline GitHub Actions CI job that runs `dart analyze` and `dart test` (or `flutter test` where applicable) across the workspace on PRs.\n\n## Constraints\n- Pin minimum Flutter / Dart SDK versions consistent with the VM service surface the host depends on (PRD §22 risk on VM service stability).\n- `exploration_agent` MUST be web-compatible — no transitive `dart:io`. This is load-bearing for §6.3 (harness runs inside the DevTools extension web app) and the §22 risk on harness-in-web-app constraints.\n- `exploration_devtools` depends on `package:devtools_extensions`; pin a minimum DevTools version (§22 DevTools version-skew risk).\n- No code-level functionality in this slice beyond what's needed for a green analyze + empty-suite test.\n\n## Ruled out\n- Single-package layout: rejected because the v0.5 architecture (PRD §6, §18) requires distinct host / harness / frontend / plugin packages with different platform constraints (Flutter vs pure Dart vs web).\n- Auto-discovery of plugins from `pubspec.yaml`: explicitly out of v1 (PRD §18 \"Not in v1\"). Plugins are user-instantiated.\n- Bundling the CLI with the harness: PRD §6.2 splits them deliberately; the harness is a library, not an executable.\n\n## Non-obvious notes\n- The web-compatibility constraint on `exploration_agent` is the trickiest invariant to keep — verify with a CI step that imports the package from a web target. Anything that needs `dart:io` (file persistence, stdio) lives in `exploration_cli`.\n- Trajectory persistence for the DevTools panel goes through the Dart Tooling Daemon's filesystem APIs (PRD §22), not `dart:io`. That work lives in cx6.21, but the package boundary set up here must already prevent the easy mistake.\n- The reference-plugin packages (`exploration_router`, `exploration_riverpod`, `exploration_dio`) only need scaffolding here — implementations come in cx6.25/.26/.27.\n- This bead blocks every other story in the epic; keep the slice tight to unblock parallel work on cx6.2/.3/.4/.5/.7 etc.","acceptance_criteria":"- [ ] Seven-package workspace with shared lints\n- [ ] All packages compile, pass analyze, and run an empty test suite\n- [ ] CI baseline runs analyze + test on PRs\n- [ ] exploration_agent has no transitive dart:io dependency (web-compatible)\n- [ ] exploration_devtools depends on package:devtools_extensions","status":"draft","priority":1,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:03Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:46:57Z","dependencies":[{"issue_id":"lenny-cx6.1","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:03Z","created_by":"Nico Spencer","metadata":"{}"}],"comments":[{"id":"019e02e2-adcd-7b13-b017-e0262c74a2cb","issue_id":"lenny-cx6.1","author":"specify","text":"BLOCKED: No approved design from the discover skill. The design field is empty; specify requires an approved design as input. Please run discover on this bead before specify.","created_at":"2026-05-07T14:41:11Z"}],"dependency_count":0,"dependent_count":13,"comment_count":1}
{"_type":"issue","id":"lenny-18f","title":"Spike: verify mlx-vlm supports the qwen3_coder tool-call parser","description":"PRD v0.5 §16.3 + §22 risk: the local-inference path depends on mlx-vlm correctly parsing Qwen3.6-35B-A3B's tool-call format. vLLM and SGLang require '--tool-call-parser qwen3_coder'; whether mlx-vlm handles the same parser format is unknown. This is a pre-build spike — its outcome shapes the local provider design and may force a fallback to mlx-lm text-only (losing vision) or moving local inference to SGLang on Linux (losing the M3 Ultra setup).\n\n## Alternatives Considered\n\n(See `design` field's \"Alternatives Considered\" section for full rationale.)\n\n- mlx-lm text-only as primary — rejected: loses vision (PRD §16.3 hard requirement).\n- SGLang on Linux as primary — rejected upfront: loses the M3 Ultra setup; chosen only if mlx-vlm fails T2 unrecoverably.\n- Custom parser shim wrapping mlx-vlm raw output — chosen if only T2 needs adapting.\n- vLLM on M3 Ultra — rejected: no working CUDA-free Metal backend at spike time.\n- Network-routed Linux inference — rejected for v0.5: extra hop and second machine.","design":"## Implementation Plan\n\n1. Create spike scaffolding — `docs/spikes/18f-mlx-vlm-toolcall.md` and `infra/mlx-vlm/`\n\n   Create the doc skeleton and infra directory:\n\n   ```bash\n   mkdir -p docs/spikes infra/mlx-vlm/scripts\n   cat \u003e docs/spikes/18f-mlx-vlm-toolcall.md \u003c\u003c'DOC'\n   # Spike 18f — mlx-vlm + qwen3_coder tool-call parser\n\n   ## Environment\n   - Host: M3 Ultra, macOS \u003cfill\u003e, Python \u003cfill\u003e\n   - mlx-vlm version: \u003cpinned\u003e\n   - Model: Qwen3.6-35B-A3B @ 8-bit (HF id: \u003cfill\u003e)\n   - Server invocation: see `infra/mlx-vlm/scripts/serve.sh`\n\n   ## Smoke Tests\n   ### T1 Text chat\n   ### T2 qwen3_coder tool call\n   ### T3 Vision image_url\n   ### T4 preserve_thinking across turns\n   ### T5 Structured output (JSON schema)\n\n   ## CORS\n   ## Sampling defaults\n   ## Decision\n   ## Pinned versions\n   DOC\n   ```\n\n   **Test:**\n   ```bash\n   test -f docs/spikes/18f-mlx-vlm-toolcall.md \u0026\u0026 test -d infra/mlx-vlm/scripts \u0026\u0026 echo OK\n   ```\n   Expected: `OK`\n\n   **Commit:**\n   ```bash\n   git add docs/spikes/18f-mlx-vlm-toolcall.md infra/mlx-vlm/\n   git commit -m \"docs(spike-18f): scaffold mlx-vlm tool-call spike\"\n   ```\n\n2. Pin mlx-vlm and stand up the server — `infra/mlx-vlm/requirements.txt`, `infra/mlx-vlm/.env.example`, `infra/mlx-vlm/scripts/serve.sh`\n\n   ```text\n   # infra/mlx-vlm/requirements.txt\n   mlx-vlm==\u003clatest-at-spike-time\u003e\n   pillow\u003e=10.0.0\n   ```\n\n   ```bash\n   # infra/mlx-vlm/.env.example\n   MLX_MODEL=mlx-community/Qwen3.6-35B-A3B-8bit\n   MLX_PORT=8080\n   MLX_HOST=127.0.0.1\n   MLX_CORS_ALLOW_ORIGIN=http://localhost:5173\n   ```\n\n   ```bash\n   # infra/mlx-vlm/scripts/serve.sh\n   #!/usr/bin/env bash\n   set -euo pipefail\n   cd \"$(dirname \"$0\")/..\"\n   set -a; source .env; set +a\n   exec python -m mlx_vlm.server \\\n     --model \"$MLX_MODEL\" \\\n     --host \"$MLX_HOST\" \\\n     --port \"$MLX_PORT\" \\\n     --temperature 1.0 --top-p 0.95 --top-k 20 \\\n     --repetition-penalty 1.0\n   ```\n\n   **Test:**\n   ```bash\n   chmod +x infra/mlx-vlm/scripts/serve.sh\n   cp infra/mlx-vlm/.env.example infra/mlx-vlm/.env\n   bash -n infra/mlx-vlm/scripts/serve.sh \u0026\u0026 echo OK\n   ```\n   Expected: `OK` (syntax check; full launch requires the box)\n\n   **Commit:**\n   ```bash\n   git add infra/mlx-vlm/\n   git commit -m \"feat(spike-18f): pin mlx-vlm and add launch script\"\n   ```\n\n3. Write smoke-test driver — `infra/mlx-vlm/scripts/smoke.py`\n\n   Hits the OpenAI-compatible endpoint for T1-T5, captures raw transcripts to `docs/spikes/18f-transcripts/T{1..5}.json`.\n\n   ```python\n   import base64, json, os, pathlib, sys, time, urllib.request\n   BASE = f\"http://{os.environ['MLX_HOST']}:{os.environ['MLX_PORT']}/v1\"\n   OUT = pathlib.Path(\"docs/spikes/18f-transcripts\"); OUT.mkdir(parents=True, exist_ok=True)\n   SAMPLING = {\"temperature\":1.0,\"top_p\":0.95,\"top_k\":20,\n               \"presence_penalty\":1.5,\"frequency_penalty\":0.0}\n\n   def post(path, body):\n       req = urllib.request.Request(BASE+path,\n           data=json.dumps(body).encode(),\n           headers={\"Content-Type\":\"application/json\"})\n       t0=time.time(); r=urllib.request.urlopen(req, timeout=120)\n       return time.time()-t0, json.loads(r.read())\n\n   def save(name, req, dt, resp):\n       (OUT/f\"{name}.json\").write_text(json.dumps(\n           {\"request\":req,\"latency_s\":dt,\"response\":resp}, indent=2))\n\n   # T1\n   req={\"model\":\"qwen\",\"messages\":[{\"role\":\"user\",\"content\":\"Say hi in 3 words.\"}],**SAMPLING}\n   dt,resp=post(\"/chat/completions\",req); save(\"T1\",req,dt,resp)\n\n   # T2 tool calls (qwen3_coder)\n   tools=[{\"type\":\"function\",\"function\":{\"name\":\"get_weather\",\n     \"parameters\":{\"type\":\"object\",\"properties\":{\"city\":{\"type\":\"string\"}},\"required\":[\"city\"]}}},\n     {\"type\":\"function\",\"function\":{\"name\":\"add\",\n     \"parameters\":{\"type\":\"object\",\"properties\":{\"a\":{\"type\":\"number\"},\"b\":{\"type\":\"number\"}},\"required\":[\"a\",\"b\"]}}}]\n   req={\"model\":\"qwen\",\"tools\":tools,\"tool_choice\":\"auto\",\n        \"messages\":[{\"role\":\"user\",\"content\":\"What is the weather in Paris and what is 2+3?\"}],**SAMPLING}\n   dt,resp=post(\"/chat/completions\",req); save(\"T2\",req,dt,resp)\n\n   # T3 vision\n   img=base64.b64encode(pathlib.Path(\"infra/mlx-vlm/scripts/fixture.png\").read_bytes()).decode()\n   req={\"model\":\"qwen\",\"messages\":[{\"role\":\"user\",\"content\":[\n     {\"type\":\"text\",\"text\":\"Describe this image in one sentence.\"},\n     {\"type\":\"image_url\",\"image_url\":{\"url\":f\"data:image/png;base64,{img}\"}}]},],**SAMPLING}\n   dt,resp=post(\"/chat/completions\",req); save(\"T3\",req,dt,resp)\n\n   # T4 preserve_thinking across two turns\n   msgs=[{\"role\":\"user\",\"content\":\"Think step by step. What is 17*23?\"}]\n   req={\"model\":\"qwen\",\"messages\":msgs,\"preserve_thinking\":True,**SAMPLING}\n   dt1,r1=post(\"/chat/completions\",req)\n   msgs.append(r1[\"choices\"][0][\"message\"])\n   msgs.append({\"role\":\"user\",\"content\":\"Now subtract 11 from that.\"})\n   req2={\"model\":\"qwen\",\"messages\":msgs,\"preserve_thinking\":True,**SAMPLING}\n   dt2,r2=post(\"/chat/completions\",req2)\n   save(\"T4\",{\"turn1\":req,\"turn2\":req2},dt1+dt2,{\"turn1\":r1,\"turn2\":r2,\"turn2_latency_s\":dt2})\n\n   # T5 structured output\n   schema={\"type\":\"object\",\"properties\":{\"city\":{\"type\":\"string\"},\"temp_c\":{\"type\":\"number\"}},\n     \"required\":[\"city\",\"temp_c\"],\"additionalProperties\":False}\n   req={\"model\":\"qwen\",\"response_format\":{\"type\":\"json_schema\",\n        \"json_schema\":{\"name\":\"weather\",\"schema\":schema,\"strict\":True}},\n        \"messages\":[{\"role\":\"user\",\"content\":\"Return JSON for Paris weather, temp 18C.\"}],**SAMPLING}\n   dt,resp=post(\"/chat/completions\",req); save(\"T5\",req,dt,resp)\n   print(\"done\")\n   ```\n\n   **Test:**\n   ```bash\n   python -c \"import ast; ast.parse(open('infra/mlx-vlm/scripts/smoke.py').read())\" \u0026\u0026 echo OK\n   ```\n   Expected: `OK`\n\n   **Commit:**\n   ```bash\n   git add infra/mlx-vlm/scripts/smoke.py\n   git commit -m \"feat(spike-18f): add T1-T5 smoke-test driver\"\n   ```\n\n4. Add image fixture and run the smoke battery — `infra/mlx-vlm/scripts/fixture.png`, `docs/spikes/18f-transcripts/`\n\n   Use any 256x256 PNG (e.g. a screenshot of a Flutter sample app) committed as `fixture.png`. Then on the M3 Ultra:\n\n   ```bash\n   pip install -r infra/mlx-vlm/requirements.txt\n   bash infra/mlx-vlm/scripts/serve.sh \u0026   # background\n   sleep 60   # wait for model load\n   set -a; source infra/mlx-vlm/.env; set +a\n   python infra/mlx-vlm/scripts/smoke.py\n   ```\n\n   **Test:**\n   ```bash\n   ls docs/spikes/18f-transcripts/T{1,2,3,4,5}.json\n   ```\n   Expected: all five files exist and are valid JSON (`python -m json.tool` on each)\n\n   **Commit:**\n   ```bash\n   git add infra/mlx-vlm/scripts/fixture.png docs/spikes/18f-transcripts/\n   git commit -m \"test(spike-18f): record T1-T5 smoke-test transcripts\"\n   ```\n\n5. Probe CORS from a browser-origin context — `docs/spikes/18f-transcripts/cors.txt`\n\n   Send a preflight + actual request with `Origin` set, capture the response headers verbatim:\n\n   ```bash\n   set -a; source infra/mlx-vlm/.env; set +a\n   {\n     echo \"=== OPTIONS preflight ===\"\n     curl -sS -i -X OPTIONS \"http://$MLX_HOST:$MLX_PORT/v1/chat/completions\" \\\n       -H \"Origin: http://localhost:5173\" \\\n       -H \"Access-Control-Request-Method: POST\" \\\n       -H \"Access-Control-Request-Headers: content-type\"\n     echo\n     echo \"=== POST with Origin ===\"\n     curl -sS -i -X POST \"http://$MLX_HOST:$MLX_PORT/v1/chat/completions\" \\\n       -H \"Origin: http://localhost:5173\" -H \"Content-Type: application/json\" \\\n       -d '{\"model\":\"qwen\",\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}]}'\n   } \u003e docs/spikes/18f-transcripts/cors.txt\n   grep -i 'access-control' docs/spikes/18f-transcripts/cors.txt || true\n   ```\n\n   If `mlx-vlm.server` lacks CORS support, document the wrapper used (e.g., a small FastAPI proxy or `caddy reverse_proxy` with `header Access-Control-Allow-Origin {origin}`) and commit its config under `infra/mlx-vlm/`.\n\n   **Test:**\n   ```bash\n   test -s docs/spikes/18f-transcripts/cors.txt \u0026\u0026 echo OK\n   ```\n   Expected: `OK`, and the file contains either native CORS headers from mlx-vlm or evidence of the proxy returning them.\n\n   **Commit:**\n   ```bash\n   git add docs/spikes/18f-transcripts/cors.txt infra/mlx-vlm/\n   git commit -m \"test(spike-18f): record CORS preflight + post probe\"\n   ```\n\n6. Fill in the spike doc with results, decision, and pinned versions — `docs/spikes/18f-mlx-vlm-toolcall.md`\n\n   For each of T1-T5, paste a 5-15 line excerpt of the transcript, mark pass/fail, and note any deviations (e.g., tool calls returned as raw text instead of structured `tool_calls[]`, vision token errors, repetition under the spec's penalty values).\n\n   Then write the Decision section using this exact decision matrix:\n\n   ```markdown\n   ## Decision\n   Choice: \u003cmlx-vlm-primary | mlx-lm-text-fallback | sglang-linux-fallback | parser-shim\u003e\n\n   Rationale (cite T1-T5):\n   - T2 tool-call parsing: \u003cobserved\u003e\n   - T3 vision: \u003cobserved\u003e\n   - T4 preserve_thinking: \u003cobserved\u003e\n   - T5 structured output: \u003cobserved\u003e\n   - CORS: \u003cnative | proxied | blocker\u003e\n\n   If mlx-vlm-primary: requires no fallback; lenny-cx6.15 implements against this server config.\n   If mlx-lm-text-fallback: lenny-cx6.15 sets capability vision=false; epic must reroute screenshot path.\n   If sglang-linux-fallback: lenny-cx6.15 targets SGLang's OpenAI endpoint with --tool-call-parser qwen3_coder; M3 Ultra path is dropped.\n   If parser-shim: lenny-cx6.15 ships a regex-based extractor that converts raw model output -\u003e tool_calls[]; document the regex here.\n\n   ## Pinned versions\n   - mlx-vlm: \u003cexact\u003e\n   - Model HF revision: \u003ccommit-sha\u003e\n   - Python: \u003cX.Y.Z\u003e\n   ```\n\n   **Test:**\n   ```bash\n   grep -E '^Choice: (mlx-vlm-primary|mlx-lm-text-fallback|sglang-linux-fallback|parser-shim)$' \\\n     docs/spikes/18f-mlx-vlm-toolcall.md\n   ```\n   Expected: a single matching line (no `\u003c...\u003e` placeholder remaining).\n\n   **Commit:**\n   ```bash\n   git add docs/spikes/18f-mlx-vlm-toolcall.md\n   git commit -m \"docs(spike-18f): record results and decision\"\n   ```\n\n7. File the decision into bead memory and unblock downstream — `bd remember` + bead comment\n\n   ```bash\n   CHOICE=$(grep -E '^Choice: ' docs/spikes/18f-mlx-vlm-toolcall.md | head -1 | awk '{print $2}')\n   MLXV=$(grep -E '^- mlx-vlm:' docs/spikes/18f-mlx-vlm-toolcall.md | awk '{print $3}')\n   bd remember \"spike-18f decision: $CHOICE; mlx-vlm=$MLXV; see docs/spikes/18f-mlx-vlm-toolcall.md\" --tag spike-18f --tag local-inference\n   bd comments add lenny-18f \"Spike complete. Decision: $CHOICE. See docs/spikes/18f-mlx-vlm-toolcall.md and infra/mlx-vlm/.\" --actor specify\n   ```\n\n   **Test:**\n   ```bash\n   bd remember list --tag spike-18f | grep -q 'spike-18f decision:' \u0026\u0026 echo OK\n   ```\n   Expected: `OK`\n\n   **Commit:** none (bd state, not git).\n\n## Touches\n\n**Files:**\n- `docs/spikes/18f-mlx-vlm-toolcall.md` — created (spike report with T1-T5, CORS, decision, pinned versions)\n- `docs/spikes/18f-transcripts/T1.json` … `T5.json` — created (raw smoke-test transcripts)\n- `docs/spikes/18f-transcripts/cors.txt` — created (preflight + POST probe output)\n- `infra/mlx-vlm/requirements.txt` — created (pinned mlx-vlm version)\n- `infra/mlx-vlm/.env.example` — created (model, host, port, CORS origin)\n- `infra/mlx-vlm/scripts/serve.sh` — created (one-command launch with sampling defaults)\n- `infra/mlx-vlm/scripts/smoke.py` — created (T1-T5 driver)\n- `infra/mlx-vlm/scripts/fixture.png` — created (vision-test image)\n\n**Symbols added/exposed:**\n- None (this is a decision spike; no application code is added).\n- Decision artifact consumed by lenny-cx6.15: `docs/spikes/18f-mlx-vlm-toolcall.md` (`Choice:` line + `## Pinned versions`) and `infra/mlx-vlm/` sample config.\n\n## Validation Plan\n\n- [ ] AC: spike doc + per-test transcripts exist → `ls docs/spikes/18f-mlx-vlm-toolcall.md docs/spikes/18f-transcripts/T{1,2,3,4,5}.json` → all listed\n- [ ] AC: T1 text chat recorded → `python -m json.tool docs/spikes/18f-transcripts/T1.json | head -20` → valid JSON with `choices[0].message.content`\n- [ ] AC: T2 tool call recorded → `python -c \"import json; d=json.load(open('docs/spikes/18f-transcripts/T2.json')); print(d['response']['choices'][0]['message'].get('tool_calls') or d['response']['choices'][0]['message'].get('content'))\"` → either structured `tool_calls` array OR raw content (the spike doc states which)\n- [ ] AC: T3 vision recorded → `python -m json.tool docs/spikes/18f-transcripts/T3.json | grep -i -E 'image|describe|content'` → response references image\n- [ ] AC: T4 preserve_thinking recorded → `python -c \"import json; d=json.load(open('docs/spikes/18f-transcripts/T4.json')); print(d['response']['turn2_latency_s'])\"` → numeric latency present\n- [ ] AC: T5 structured output recorded → `python -c \"import json,jsonschema; d=json.load(open('docs/spikes/18f-transcripts/T5.json')); c=json.loads(d['response']['choices'][0]['message']['content']); jsonschema.validate(c,{'type':'object','properties':{'city':{'type':'string'},'temp_c':{'type':'number'}},'required':['city','temp_c']})\"` → no exception (or spike doc records why T5 failed)\n- [ ] AC: decision recorded → `grep -E '^Choice: (mlx-vlm-primary|mlx-lm-text-fallback|sglang-linux-fallback|parser-shim)$' docs/spikes/18f-mlx-vlm-toolcall.md` → one matching line\n- [ ] AC: CORS documented → `grep -i 'Access-Control-Allow-Origin' docs/spikes/18f-mlx-vlm-toolcall.md` → present\n- [ ] AC: sampling defaults present in committed config → `grep -E 'temperature 1.0|top-p 0.95|top-k 20' infra/mlx-vlm/scripts/serve.sh` → all three matched, and spike doc T1 transcript shows non-empty, non-runaway output\n- [ ] AC: sample server config runnable → `bash -n infra/mlx-vlm/scripts/serve.sh \u0026\u0026 test -f infra/mlx-vlm/.env.example \u0026\u0026 test -f infra/mlx-vlm/requirements.txt` → exit 0\n- [ ] AC: bd remember entry filed → `bd remember list --tag spike-18f | grep -q 'spike-18f decision:'` → match\n\n## Alternatives Considered\n\n- **mlx-lm text-only as primary.** Rejected upfront: PRD §16.3 requires vision for the screenshot-driven observation loop. Falls back to this only if T3 (vision) is the sole failing test and T2 passes — then lenny-cx6.15 must drop screenshot capability and the epic must reroute.\n- **SGLang on Linux as primary.** Rejected upfront: loses the M3 Ultra setup the user has standing. Becomes the choice if T2 (tool-call parsing) fails on mlx-vlm in a way no shim can fix and T3/T5 don't both pass either, since SGLang already supports `--tool-call-parser qwen3_coder` natively.\n- **Custom parser shim wrapping mlx-vlm raw output.** Considered if T2 returns the qwen3_coder XML/JSON envelope as `content` rather than structured `tool_calls[]`. Cheaper than switching servers, but adds maintenance surface. Selected only if T1, T3, T4, T5 all pass and only T2 needs adapting.\n- **vLLM on the M3 Ultra.** Rejected: vLLM does not have a working CUDA-free Metal backend for Qwen3.6-A3B at spike time; out of scope.\n- **Routing all local inference through a Linux box over the network.** Rejected for v0.5: adds a network hop and a second machine to the deployment story; revisit only if SGLang-on-Linux becomes the chosen fallback.","acceptance_criteria":"- [ ] `docs/spikes/18f-mlx-vlm-toolcall.md` exists and contains: environment, model, mlx-vlm version, server invocation, and one section per smoke test (T1-T5) with raw request/response transcripts\n- [ ] T1 (text-only chat) recorded: pass/fail with model output excerpt\n- [ ] T2 (qwen3_coder tool call with two simple tools) recorded: pass/fail, parsed `tool_calls[]` JSON shown, plus raw model output for comparison\n- [ ] T3 (image_url base64 vision input) recorded: pass/fail with model output excerpt referencing image content\n- [ ] T4 (preserve_thinking=true across two turns) recorded: pass/fail with second-turn latency and evidence of cache reuse or absence thereof\n- [ ] T5 (structured output via JSON schema / response_format) recorded: pass/fail with returned JSON and schema-validation result\n- [ ] Decision section in the doc states one of: `mlx-vlm-primary`, `mlx-lm-text-fallback`, `sglang-linux-fallback`, `parser-shim` — with one-paragraph rationale citing T1-T5 outcomes\n- [ ] CORS section documents exact headers (`Access-Control-Allow-Origin`, `-Methods`, `-Headers`) needed for the DevTools panel and how they are set for the chosen path\n- [ ] Sampling defaults (`temperature=1.0`, `top_p=0.95`, `top_k=20`, `presence_penalty=1.5`, `repetition_penalty=1.0`) are present in the committed sample server config and the spike doc confirms a generation produced output without runaway repetition or empty completions\n- [ ] `infra/mlx-vlm/` (or `infra/\u003cchosen-path\u003e/`) contains a runnable sample config: launch script, env file template, and README with one-command start\n- [ ] `bd remember` entry filed under tag `spike-18f` summarizing the decision and the exact mlx-vlm (or fallback) version pinned","status":"planned","priority":1,"issue_type":"decision","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:55:58Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:44:30Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.35","title":"Plugin authoring guide","description":"v1 documentation deliverable per PRD §19. Covers: the plugin contract with worked examples, the three reference plugins as readable source, conventions (namespace selection, observation budget management, when to report busy, plugin-owned identifier surfacing), anti-patterns (subclassing the binding, hogging frame callbacks, unbounded fragments, swallowing exceptions), and the §20.2 taxonomy (configuration / composition / subclass / reimplementation plugins) for wrapping existing tools. Includes the extension/exploration/config.yaml manifest convention. The guide can be drafted from the contract alone; reference-plugin worked examples are slotted in as those plugins land.","design":"## Approach\nv1 documentation deliverable per PRD §19: a standalone plugin authoring guide written so a third party can ship a working plugin from the guide alone, with no need to read host source (success criterion §23). Structure: (1) the §7 plugin contract walkthrough — `ExplorationPlugin` interface, `ExplorationTool`, observation fragment shape, `BusyState`, `PluginContext` lifecycle hooks — each with a small inline example; (2) the three reference plugins (lenny-cx6.25, .26, .27) linked as canonical readable source, each illustrating a different plugin shape (action contribution, structured state observation, busy-state hook); (3) conventions: namespace selection (`^[a-z][a-z0-9_]*$`, unique per session), observation budget management (default 1KB serialized per plugin, truncate gracefully, return null when nothing relevant), when to report busy (only for plugin-known async work the host can't see — frame work and animations are already covered by the host), plugin-owned identifier surfacing (the §21 default convention: each plugin's observation fragment includes the IDs it'll accept as inputs to its tools); (4) anti-patterns: subclassing the binding, hogging frame callbacks via `registerFrameCallback`, returning unbounded fragments, swallowing exceptions inside `observe()`/`busyState()`; (5) the §20.2 taxonomy with worked guidance for each category — configuration, composition, subclass, reimplementation; (6) the `extension/exploration/config.yaml` manifest convention from §7.6 (declares namespace, Dart class, constructor requirements; not read by the host in v1 but adopting now keeps packages discoverable when v2 auto-discovery lands); (7) the §7.7 versioning posture — adding tools, expanding fragments, refining busy-state heuristics are non-breaking changes so plugin authors should release as often as they want. The guide ships as Markdown in the repo with a stable URL that the AGENTS.md template (lenny-cx6.20) and DevTools panel (lenny-cx6.21) can both link to.\n\n## Constraints\nThe guide can be drafted from lenny-cx6.3 (contract types) ALONE — that's the entire dependency, deliberately, per the v0.5 tightening pass. Reference-plugin worked examples slot in as those plugins (lenny-cx6.25/26/27) land; the guide ships incrementally, not gated on all three reference plugins being complete. The guide MUST be readable without access to host internals — anything a plugin author needs that's only in host source is a bug in the contract or the guide. References to specific PRD sections should cite §-numbers (the PRD is canonical; the guide is the on-ramp).\n\n## Ruled out\nGenerating the guide automatically from doc-comments on the contract types (e.g. dartdoc) is rejected for v1 — the narrative shaping (anti-patterns, taxonomy guidance, identifier convention rationale) is the actual deliverable and doesn't come from API docs. Bundling a `exploration_plugin create` scaffolding generator into v1 is rejected — that's deferred to lenny-cx6.32 (P3, deferred 60d). Waiting for all three reference plugins to land before shipping any guide is rejected per the v0.5 tightening pass — guide depends only on .3, examples slot in incrementally.\n\n## Non-obvious notes\nPer the v0.5 tightening pass: this bead's dependency was deliberately reduced to lenny-cx6.3 alone (not lenny-cx6.25/26/27) so guide drafting can run in parallel with reference plugin implementation. The taxonomy in §20.2 (configuration / composition / subclass / reimplementation) is the load-bearing decision tree for plugin authors who want to wrap an existing Flutter package — every taxonomy category needs a worked example, with Marionette as the case study for subclass-vs-reimplementation tension (§20.1). The §7.6 manifest convention (extension/exploration/config.yaml) aligns with Jake MacDonald's Packaged AI Assets proposal for the Dart MCP server — a package shipping both extension/mcp/config.yaml and extension/exploration/config.yaml gets coding-time and runtime agent integration in one package; document this alignment, it's an ecosystem coherence story, not just a file format. Custom-widget plugin guidance (per acceptance criterion) addresses sparse-semantics apps on custom design systems where the host's diagnostic (lenny-cx6.10) points users at this guide for the fix. Trajectory plugin metadata format (lenny-cx6.19) and dynamic tool list construction (lenny-cx6.14) should be linked but not duplicated — the guide is about authoring, not internals.","acceptance_criteria":"- [ ] Contract reference complete with worked examples\n- [ ] Reference plugins linked as canonical readable source (slotted in as they land)\n- [ ] Conventions and anti-patterns documented\n- [ ] §20.2 taxonomy explained with examples for each category\n- [ ] config.yaml manifest convention specified\n- [ ] §7.7 versioning posture documented (plugin updates = non-breaking)\n- [ ] A third party can ship a working plugin from the guide alone (no host source needed)\n- [ ] Recommends writing a custom-widget plugin for apps with sparse semantics on custom design systems","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T14:30:21Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:49:38Z","dependencies":[{"issue_id":"lenny-cx6.35","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T09:30:21Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.35","depends_on_id":"lenny-cx6.3","type":"blocks","created_at":"2026-05-07T09:30:21Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.26","title":"exploration_riverpod reference plugin","description":"Reference plugin demonstrating structured state contribution. observe() emits {invalidatable_providers, recent_state_changes}. Tool: riverpod.invalidate_provider(provider_id) to force-refresh state for adversarial/edge-case testing. Constructor takes the app's ProviderContainer. Validates that plugin-owned identifiers (provider IDs) are surfaced through observation. PRD §6.4, §12.2, §21.","design":"## Approach\nReference plugin under namespace `riverpod` that demonstrates structured state contribution. Constructor takes the app's `ProviderContainer` (passed in explicitly at registration time per §7.6 — no global lookup, no auto-discovery). `observe()` returns `{invalidatable_providers: [...], recent_state_changes: [...]}` where `invalidatable_providers` is a list of provider IDs currently held in the container that are valid arguments to the plugin's `invalidate_provider` tool, and `recent_state_changes` is a short ring of recently-changed providers (cheap diff signal complementing the host's semantics tree diff). Provider IDs follow Riverpod's debugLabel convention with a fallback to the runtime type name; the plugin documents the format and uses it consistently across the fragment and the tool input. Contributes one tool: `riverpod.invalidate_provider(provider_id)` which calls `container.invalidate(provider)` — primarily useful for adversarial / edge-case testing where the agent wants to force a refresh. `busyState()` reports not-busy (Riverpod state changes drive widget rebuilds which the host's frame-stability signal already covers). No lifecycle hook contributions beyond `initialize`/`dispose`.\n\n## Constraints\nImplements the §7 plugin contract from lenny-cx6.3. Default per-plugin observation budget 1KB serialized — `invalidatable_providers` list MUST be truncated to fit (most relevant N, with a `truncated: true` marker) rather than emitted unbounded. `observe()` MUST return null when the container has no providers to surface (e.g. very early in startup). Tool input schema validates `provider_id` as a string. The plugin MUST NOT subclass the Flutter binding or install a global Riverpod observer that conflicts with what the app already has wired up — instead it attaches a `ProviderObserver` to the supplied container.\n\n## Ruled out\nAuto-discovering the `ProviderContainer` from a global is rejected — plugins are user-instantiated per §7.6, the container is passed in at registration. Reflecting over Riverpod internals to enumerate every provider type ever declared is rejected in favor of \"what's actually in the container right now\" — keeps the fragment scoped to what the agent can actually act on. Trying to capture full state values in the fragment is rejected; only IDs and change signals — full state is too large for the budget and the agent can probe specific values via Riverpod's existing public API if needed.\n\n## Non-obvious notes\nThis is the canonical demonstration of the §21 plugin-owned identifier convention: provider IDs are surfaced through `invalidatable_providers` in the observation fragment, and the tool `invalidate_provider` consumes the same ID format. The plugin authoring guide (lenny-cx6.35) cites this plugin as the worked example for \"how do I surface my plugin's identifiers so the model knows what to pass to my tools\". Depends on lenny-cx6.3 (contract) and lenny-cx6.11 (harness session) per build order. Sibling plugins: lenny-cx6.25 (router, action contribution), lenny-cx6.27 (dio, busy-state). Risk per §22: \"plugin authors disagree about identifiers\" — provider IDs vs route names vs request IDs — namespacing keeps them distinct. Ordering pitfall: do not register the `ProviderObserver` until `initialize(PluginContext)` is called — early registration races the app's container construction.","acceptance_criteria":"- [ ] Plugin registers under namespace 'riverpod'\n- [ ] observe() lists invalidatable providers and recent state changes\n- [ ] invalidate_provider tool refreshes the targeted provider\n- [ ] Provider identifier convention documented and surfaced in the observation fragment","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:21Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:38Z","dependencies":[{"issue_id":"lenny-cx6.26","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:20Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.26","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:09Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.26","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:41Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.26","depends_on_id":"lenny-cx6.3","type":"blocks","created_at":"2026-05-07T09:01:40Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.27","title":"exploration_dio reference plugin (validates busy-state mechanism)","description":"Reference plugin demonstrating busy-state contribution AND action contribution. observe() emits {in_flight: [...], recent_completed: [...]}. busyState() reports busy with reason='N in-flight requests' and est_ms while requests are pending — this is the mechanism that solves 'wait for in-flight network' for stability policies. Tool: dio.cancel_in_flight() to cancel pending requests for adversarial testing. Validates the busy-state hook end-to-end. PRD §7.4, §18.","design":"## Approach\nReference plugin under namespace `dio` that is the LOAD-BEARING end-to-end validator for the §7.4 busy-state mechanism. Constructor takes the app's configured `Dio` instance. The plugin attaches a Dio `Interceptor` that tracks in-flight requests (start/complete/error) in an internal map keyed by request ID. `observe()` returns `{in_flight: [...], recent_completed: [...]}` where each in-flight entry includes request ID, method, URL host+path (query stripped), elapsed ms, and a best-effort estimated remaining ms; `recent_completed` is a short ring of the last few completions with status code and duration. `busyState()` reports `isBusy=true, reason=\"N in-flight requests\", estimatedDuration=max remaining` while any request is pending — this is the mechanism that solves \"wait for in-flight network\" for the action-relative and quiet-frame stability policies (PRD §7.4, §9). Contributes one tool: `dio.cancel_in_flight()` for adversarial testing — calls `CancelToken.cancel()` across all tracked tokens. The end-to-end validation: drive an action that triggers a Dio request, observation capture must correctly delay until the request completes and `busyState()` returns not-busy, then the observation reflects post-network state.\n\n## Constraints\nImplements the §7 plugin contract from lenny-cx6.3. Default per-plugin observation budget 1KB serialized — long URLs and large in-flight counts MUST be truncated. `observe()` MUST return null when there are no in-flight or recently-completed requests AND no useful state to report (steady state). The interceptor must be installed in `initialize(PluginContext)` AFTER the app's interceptors so the plugin observes everything the app sees. The plugin MUST NOT depend on the host's frame stability signal for its own busy reporting — `busyState()` is independent and authoritative for network-pending status. URL query strings MUST be stripped from observation to avoid leaking auth tokens or PII into the trajectory.\n\n## Ruled out\nWrapping the http package or Chopper instead of Dio is rejected for v1 — Dio is the most common Flutter networking choice and a single reference plugin is enough to prove the busy-state mechanism. Reporting busy based on time-since-last-request (heartbeat) is rejected; only actually-in-flight requests should hold the stability budget. Surfacing full request bodies is rejected (privacy, budget); only metadata. Subclassing the binding to install a global HttpClient interceptor is explicitly anti-pattern per §19 — the plugin uses Dio's own interceptor API.\n\n## Non-obvious notes\nThis bead is the only place in v1 where the busy-state contract is exercised end-to-end — lenny-cx6.25 (router) and lenny-cx6.26 (riverpod) both report not-busy. Acceptance criterion \"action-relative stability correctly delays observation until requests complete\" is the success criterion §23 explicitly calls out (≥95% terminate within budget including correct dio waits). The plugin-owned identifier convention applies here too — `cancel_in_flight()` could be extended to accept request IDs surfaced in the observation, but v1 keeps it as cancel-all to keep the validator focused on the busy-state mechanism. Plugin authoring guide (lenny-cx6.35) cites this as the canonical busy-state example. Depends on lenny-cx6.3 (contract) and lenny-cx6.11 (harness session) per build order; the action-relative stability policy lives in lenny-cx6.8 and is the integration partner. Risk per §22 mitigation: actually try this against a real app with real network calls before declaring the busy-state design done.","acceptance_criteria":"- [ ] Plugin registers under namespace 'dio'\n- [ ] observe() reports in-flight and recent-completed requests\n- [ ] busyState() returns isBusy=true with reason while requests pending\n- [ ] action-relative stability correctly delays observation until requests complete\n- [ ] cancel_in_flight tool cancels pending requests","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:21Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:49:03Z","dependencies":[{"issue_id":"lenny-cx6.27","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:21Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.27","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:10Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.27","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:42Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.27","depends_on_id":"lenny-cx6.3","type":"blocks","created_at":"2026-05-07T09:01:42Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.25","title":"exploration_router reference plugin (raw Router/Navigator)","description":"Reference plugin demonstrating routing observation + action contribution. Targets Flutter's built-in Router and Navigator primitives directly — no third-party routing dependency. observe() emits {current_route_name, stack: [...], arguments}. Tool: router.navigate(route_name, arguments?) for programmatic navigation that bypasses UI hit-testing. Stresses the contract honestly (no clean RouterDelegate API to lean on). PRD §18, §6.4.","design":"## Approach\nReference plugin under namespace `router` that targets Flutter's built-in `Router` (declarative, with `RouterDelegate` / `RouteInformationParser`) and `Navigator` (imperative) primitives directly — no third-party routing framework dependency. Constructor takes the app's root `GlobalKey\u003cNavigatorState\u003e` (and optionally a `RouterDelegate` reference) so the plugin can introspect the current route stack from either world. `observe()` returns a fragment of shape `{current_route_name, stack: [...], arguments}` for both imperative `Navigator.push` stacks and declarative `Router` configurations; mixed apps are observed correctly because the plugin reads from whichever surface is live. Contributes one tool: `router.navigate(route_name, arguments?)` which performs programmatic navigation that bypasses UI hit-testing — the model uses this when it already knows where it wants to go and shouldn't have to drive there through taps. `busyState()` reports not-busy (routing transitions are picked up by the host's frame stability signal in lenny-cx6.4). No lifecycle hook contributions beyond `initialize`/`dispose`. The plugin's observation fragment is what the agent reads to learn valid `route_name` values it can pass back into `router.navigate` — this is the canonical worked example of the §21 plugin-owned identifier convention.\n\n## Constraints\nImplements the §7 plugin contract from lenny-cx6.3. Default per-plugin observation budget 1KB serialized (truncated with warning if exceeded). `observe()` MUST return null when the app is not currently on a route this plugin can observe (e.g. during initial boot before the router is wired). Tool name MUST be namespaced as `router.navigate`. Tool input schema is JSON-Schema validated by the harness regardless of plugin source.\n\n## Ruled out\nv0.5 changelog explicitly REPLACED `go_router` with raw `Router`/`Navigator` as the routing reference plugin. Rationale per PRD §18: simpler, exercises the contract more honestly (no clean `RouterDelegate` API to lean on — the plugin must work against whatever the user has wired up), avoids picking a winner among third-party routers, no opinionated framework dependency forced on the host. Validates that the contract can express the most general case; framework-specific plugins (`go_router`, `auto_route`, `beamer`) become straightforward to author by following this pattern. Also ruled out: a unified routing abstraction in the host core (per §6.4, this is \"unsolvable in the core\" — routing diversity is the founding motivation for the plugin architecture).\n\n## Non-obvious notes\nPlugin-owned identifier convention (PRD §21): the observation fragment surfaces the route names the agent can pass into `router.navigate` — this is the canonical demonstration that plugins surface their own identifiers through their observation fragment. Acts as the worked example referenced from the plugin authoring guide (lenny-cx6.35). Depends on lenny-cx6.3 (plugin contract) and lenny-cx6.11 (harness session API for end-to-end validation) per the build order. Sibling reference plugins lenny-cx6.26 (riverpod, structured state) and lenny-cx6.27 (dio, busy-state) round out the three-plugin coverage that proves the contract works across all three plugin shapes (observation, action contribution, lifecycle hook). Ordering pitfall: this plugin makes no busy-state contribution — that mechanism is validated by lenny-cx6.27, not here.","acceptance_criteria":"- [ ] Plugin registers under namespace 'router'\n- [ ] observe() returns routing fragment for both imperative Navigator and declarative Router apps\n- [ ] router.navigate tool routes correctly when the destination is registered\n- [ ] Plugin works against an app that mixes Navigator pushes and Router config\n- [ ] Returns null fragment when not on a managed route","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:20Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:14Z","dependencies":[{"issue_id":"lenny-cx6.25","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:19Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.25","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:09Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.25","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:40Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.25","depends_on_id":"lenny-cx6.3","type":"blocks","created_at":"2026-05-07T09:01:39Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.24","title":"DevTools: Timeline panel (live + post-session browsing)","description":"Scrollable list of turns. Each row: action taken, one-line diff summary, running summary at that point. Click expands into detail view: full observation (semantics, routes, plugin fragments, screenshot if enabled), reasoning trace, action, validation. Updates live during session; browsable after end. PRD §6.3, §23.","design":"## Approach\nBuild the timeline sub-panel as a virtualized scrollable list of turn rows backed by a `List\u003cTurnRecord\u003e` that grows during a live session and is loadable from disk for post-session browsing. The data source is a unified `TurnRecord` value type: live records come from `ExplorationSession`'s per-turn event stream (action executed → diff computed → summary updated, all from .18); post-session records come from parsing the JSONL trajectory format produced by .19, read via DTD filesystem APIs (per .21's persistence path). Each row renders three lines: action name + args one-liner, mechanical diff one-liner (truncated structural-delta from .12), and the running summary at that turn. Tapping a row pushes a detail view that renders the full turn record: full observation (semantics tree, route stack, plugin fragments rendered as expandable JSON nodes, screenshot if attached), the model's full reasoning trace, action + args, and validation result from .17. Use `ListView.builder` for virtualization so a 30-min session (~50MB trajectory per PRD §23) scrolls without performance degradation. A mode toggle at the top selects between \"Live\" (current session, append-only) and \"Browse\" (load JSONL file via DTD file picker).\n\n## Constraints\n- Must read the trajectory JSONL format produced by .19 — schema must align exactly, including plugin contribution metadata and fragment versions.\n- Live updates must not rebuild the full list — use keyed ListView items and append-only state mutations.\n- Performance budget: 30-min session trajectories (PRD §23 success criterion: \u003c50MB, browsable in panel without performance degradation).\n- All file I/O via DTD APIs, never `dart:io` (inherited from .21 / PRD §22).\n- Detail view must lazily render large fields (full semantics tree, screenshot bytes) so the row tap is responsive.\n\n## Ruled out\n- A separate live-only vs post-session UI — rejected; unifying both modes through one `TurnRecord` shape avoids two parallel render paths.\n- Long-form trajectory analytics (charts, aggregate stats, search) — explicitly out of v1 per PRD §6.3 and §18 (\"DevTools panel features beyond the three v1 sub-panels\").\n- Multi-session comparison — out of v1.\n- Rendering the full observation inline in each row — rejected; budget pressure and scroll perf forbid it. Detail view on tap is the v1 pattern.\n\n## Non-obvious notes\n- Depends on .19 (trajectory JSONL writer) AND .21 (scaffold) — both must land before this can complete the post-session browsing path.\n- The diff one-liner per row is `core` + plugin diff summarization from .12; align the truncation rule (e.g., \"+3 nodes, route → /checkout\") with whatever .12 produces so the row stays single-line.\n- Coordinate with .23 on the shared per-turn event type — both panels are downstream of the same session event stream, and a single typed stream avoids duplicate plumbing.\n- Screenshot rendering must handle the vision-on-by-default case from PRD §11.1 — base64-decoded image bytes from the trajectory record, lazy-loaded.","acceptance_criteria":"- [ ] Turn rows render with action, diff one-liner, summary\n- [ ] Detail view shows full observation, reasoning, action, validation\n- [ ] Live updates while session runs without performance degradation on a 30-min session\n- [ ] Post-session browsing of an in-panel trajectory works","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:19Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:51Z","dependencies":[{"issue_id":"lenny-cx6.24","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:18Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.24","depends_on_id":"lenny-cx6.19","type":"blocks","created_at":"2026-05-07T09:01:38Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.24","depends_on_id":"lenny-cx6.21","type":"blocks","created_at":"2026-05-07T09:01:37Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.23","title":"DevTools: Thinking panel (live streaming)","description":"Streams the model's reasoning trace into a scrollable pane as it generates. For Qwen3.6, this is the \u003cthink\u003e...\u003c/think\u003e content emitted before the action. Surfaces the action ultimately selected and the validation result. Must keep up with generation rate on M3 Ultra hardware (no perceptible token-display lag) per PRD §23. PRD §6.3.","design":"## Approach\nBuild the thinking sub-panel as a scrollable text view that consumes a `Stream\u003cString\u003e` of reasoning tokens from the active `ExplorationSession`. The provider abstraction from .14 must expose a streaming API (`Stream\u003cModelChunk\u003e`) end-to-end: the MLX provider from .15 streams `\u003cthink\u003e...\u003c/think\u003e` content from Qwen3.6 over the OpenAI-compatible HTTP streaming endpoint (Server-Sent Events), and the harness loop in .18 forwards each chunk to subscribers without buffering. The panel widget subscribes to the per-turn token stream, appends to a render buffer, and pushes characters into a `SelectableText` inside a `ListView` (or a `CustomScrollView` for performance on long traces). After the model finishes generating for a turn, the panel renders two trailing lines: the action selected (tool name + args summary) and the validation result from .17 (success or error). Auto-scroll follows the bottom by default; if the user scrolls up manually, auto-scroll pauses and a \"jump to live\" affordance appears.\n\n## Constraints\n- Must use the provider's **streaming** API end-to-end — no buffer-then-emit. Per PRD §23, the thinking-trace stream must keep up with M3 Ultra generation rate with no perceptible lag.\n- Rendering must avoid full-tree rebuilds per token — use a controller-driven append pattern (e.g., `ValueListenable\u003cStringBuffer\u003e` or an append-only `TextEditingController`) so token throughput is decoupled from widget rebuild cost.\n- Panel inherits .21's web-only constraint (no `dart:io`).\n- For HTTP streaming to local MLX from a web app, CORS configuration must allow event-stream responses — this is documented in .21 but worth re-validating when wiring.\n- Manual scroll-up must reliably pause auto-scroll; auto-scroll re-engages only on explicit user action.\n\n## Ruled out\n- Out-of-process streaming over a separate channel — rejected; PRD §6.3's in-panel run shares the in-memory session state, including the token stream, directly.\n- Buffering full reasoning trace per turn before rendering — rejected; defeats the v0.5 KEY USER VALUE of real-time visibility into the model's mental model of the app.\n- Markdown / rich rendering of reasoning — deferred; plain monospace text is sufficient for v1 and avoids per-token reformat cost.\n\n## Non-obvious notes\n- This is the v0.5 KEY USER VALUE described in PRD §6.3 (changes from v0.4) — the differentiator vs CLI. Performance is non-negotiable.\n- Validate streaming throughput specifically against MLX-served Qwen3.6-35B-A3B on the M3 Ultra target hardware (PRD §23), not just synthetic streams.\n- The action+validation suffix bridges this panel to the timeline panel (.24); consider emitting a single shared event type so both panels consume the same stream.\n- Mid-turn interrupt (.31) will hook into this stream's cancellation; keep the Stream subscription lifecycle clean.","acceptance_criteria":"- [ ] Reasoning tokens stream into the panel as the model generates\n- [ ] No perceptible lag between token production and display on M3 Ultra\n- [ ] Selected action and validation result surfaced after generation\n- [ ] Auto-scroll with manual override\n- [ ] Provider's streaming API is used end-to-end","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:18Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:25Z","dependencies":[{"issue_id":"lenny-cx6.23","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:17Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.23","depends_on_id":"lenny-cx6.21","type":"blocks","created_at":"2026-05-07T09:01:37Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.22","title":"DevTools: Prompt panel","description":"Goal text field, Start button, model selection dropdown, session-budget controls (turns / wall-clock), plugin-enablement toggles populated from the connected app's registered plugins. Hitting Start instantiates an ExplorationSession in-panel. Replaces the CLI's argument parsing for interactive use. PRD §6.3.","design":"## Approach\nBuild the prompt sub-panel as a Flutter widget rendered into the Prompt tab placeholder from .21. The layout is a single-column form: a multiline goal `TextField`, a model selection `DropdownButton` (populated from the providers registered in .14/.15/.16), session-budget controls (turn count and wall-clock duration with sensible defaults from PRD §10), a `CheckboxListTile` group for plugin enablement, and a `Start` button. Plugin toggles populate from the binding handshake established in .11 — when the panel connects to the running app, it queries the registered plugin list (namespaces, descriptions) via the host's VM service extensions and renders a toggle per plugin, with all enabled by default. Hitting Start collects the form state into an `ExplorationSessionConfig`, instantiates `ExplorationSession` in-panel (per .21's in-panel-run model), and disables form inputs while the session runs. A Stop button replaces Start during runtime to allow clean session termination via the loop driver's cancellation hook from .18.\n\n## Constraints\n- Plugin list is dynamic — must come from the live binding handshake, not a static config file. If the binding reports zero plugins, render an empty plugin section with a hint pointing at the plugin authoring guide (.35).\n- Form must be disabled (not just visually grayed) while a session is running to prevent mid-session config changes.\n- Stop control must invoke the same cancellation path the .31 mid-turn-interrupt feature will eventually extend — design the Stop wiring to be extensible rather than ad-hoc.\n- Web-only constraints inherited from .21 (no `dart:io`).\n- Model selection list must reflect only providers actually configured/available in the running harness, not a hardcoded superset.\n\n## Ruled out\n- Replicating the CLI's full argument-parsing surface in this UI — rejected as out of scope per the deliberately minimal v1 scope (PRD §6.3: \"deliberately does not duplicate everything the CLI does\"). Advanced flags stay CLI-only.\n- A wizard / multi-step flow — rejected; single-page form is faster for the iterative dev usage pattern this targets.\n- Persisting form state across DevTools sessions — deferred; not a v1 requirement.\n\n## Non-obvious notes\n- Depends only on .21 (scaffold); the binding handshake itself is .11's responsibility — confirm .11 surfaces `registeredPlugins` before starting this bead.\n- This panel is the user-visible entry point referenced in PRD §23's \"usable prompt-and-watch loop within 30 seconds\" success criterion.\n- Design the form-state model as a freezed/value-class so the .31 follow-up (mid-turn interrupt + hint injection) can layer on without restructuring.","acceptance_criteria":"- [ ] Goal entry, model selection, budgets, and plugin toggles render\n- [ ] Start spins up an in-panel ExplorationSession\n- [ ] Plugin toggles reflect registered plugins from the binding handshake\n- [ ] Disabled inputs while a session runs; clear stop control","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:17Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:06Z","dependencies":[{"issue_id":"lenny-cx6.22","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:16Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.22","depends_on_id":"lenny-cx6.21","type":"blocks","created_at":"2026-05-07T09:01:36Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.21","title":"DevTools extension scaffold and auto-discovery","description":"exploration_devtools as a Flutter web app embedded in DevTools via package:devtools_extensions. Auto-discovery: when the user's app depends on exploration_flutter, DevTools shows an 'Exploration' tab in standalone, VS Code, and Android Studio integrations. Provides theming, VM service connection inheritance, and the empty panel shell. Per PRD §22, harness runs IN-PANEL (no extra IPC) and trajectories persist via Dart Tooling Daemon filesystem APIs. PRD §6.3, §18.","design":"## Approach\nCreate `exploration_devtools` as a Flutter web app structured per the `package:devtools_extensions` template: `extension/devtools/config.yaml` declares the extension; `lib/main.dart` boots a `DevToolsExtension` widget that owns theming and the inherited VM service connection. Auto-discovery is config-driven — declaring the extension in `exploration_flutter`'s `extension/devtools/` directory means standalone DevTools, VS Code DevTools, and Android Studio DevTools all surface the \"Exploration\" tab automatically when the user's app pubspec transitively depends on `exploration_flutter`. The scaffold ships an empty panel shell with a tabbed layout (Prompt / Thinking / Timeline placeholders) wired to a single in-panel `ExplorationSession` instance from .11; sub-panels (.22/.23/.24) populate the placeholders. Trajectory persistence routes through the Dart Tooling Daemon (DTD) filesystem APIs exposed by the extension framework — never `dart:io` — for both writes during live sessions and reads for post-session browsing. The session itself runs in-panel (no extra harness process); the panel is the harness host and the renderer.\n\n## Constraints\n- DevTools extension is a Flutter **web** app; the entire transitive code path from this package CANNOT depend on `dart:io`. Trajectory I/O must use DTD filesystem APIs per PRD §22.\n- Pin a minimum `package:devtools_extensions` version and pin minimum DevTools version in `extension/devtools/config.yaml`; this API is still evolving (PRD §22).\n- CORS may be required for browser-originated requests to local MLX inference; document the required `mlx-vlm`/SGLang/vLLM headers in the package README so users can configure their server.\n- Auto-discovery must not require additional dev-dependency configuration in user pubspecs — declaring `exploration_flutter` as a dependency is enough.\n- VM service connection must be inherited from DevTools, not opened independently.\n\n## Ruled out\n- Out-of-process harness with the panel as a thin UI client over IPC — rejected per PRD §6.3; in-panel run avoids a second IPC layer and shares in-memory `ExplorationSession` state directly.\n- Direct disk writes via `dart:io` from the panel — rejected; PRD §22 mandates DTD APIs.\n- Manual user activation of the panel — rejected; PRD §23 requires auto-discovery within 30s of opening DevTools.\n\n## Non-obvious notes\n- This bead is the gating dependency for .22/.23/.24; finishing the scaffold (including DTD persistence path) unblocks all three sub-panels in parallel.\n- PRD §22 explicitly identifies devtools_extensions API churn as a known risk and accepts a minor follow-up release — bake a version-pin strategy in from day one.\n- The harness library (.11+) is web-compatible by construction (PRD §22 risk mitigation); validate that no `dart:io` leaked into transitive deps before declaring the scaffold done.","acceptance_criteria":"- [ ] Package builds as a DevTools extension\n- [ ] Auto-discovered when user's app depends on exploration_flutter\n- [ ] Tab appears in standalone DevTools, VS Code, and Android Studio\n- [ ] VM service connection inherited from DevTools\n- [ ] DTD-based trajectory persistence path implemented and tested in-panel","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:16Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:47:41Z","dependencies":[{"issue_id":"lenny-cx6.21","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:16Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.21","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:35Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.21","depends_on_id":"lenny-cx6.18","type":"blocks","created_at":"2026-05-07T09:01:34Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.21","depends_on_id":"lenny-cx6.19","type":"blocks","created_at":"2026-05-07T09:01:35Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":3,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.20","title":"CLI frontend (exploration_cli) with AGENTS.md template","description":"exploration_cli command-line tool. Goal as argument or stdin, --vm-uri, --model, --output, --policy, --plugins (comma-separated). Streams progress to stdout, writes trajectory to disk via dart:io. Right interface for CI runs and batch trajectory collection. Includes a minimal AGENTS.md template under templates/ that --help references. PRD §6.2 (CLI frontend), §18 (v1 deliverable).","design":"## Approach\nAdd `exploration_cli` as a Dart console app under the cli/ package (created in .1). Use `package:args` for flag parsing: `--vm-uri`, `--model`, `--output`, `--policy`, `--plugins`, plus `--goal` (or read from stdin if absent and stdin is not a TTY). The CLI is a thin shell around `ExplorationSession` from .11: parse flags, resolve per-tier model defaults, instantiate the appropriate `ModelProvider` from .14/.15/.16, attach a stdout progress listener, and run `session.run()` to completion. Trajectory is written to disk via the .19 writer using `dart:io` directly. AGENTS.md template lives at `exploration_cli/templates/AGENTS.md` and is referenced from `--help` output (exact relative path printed) so users can copy and adapt it. Per-tier model defaults are a static map (qwen-mlx → local MLX defaults, claude → Sonnet defaults, openai → GPT-5-class defaults) consistent with PRD §16.4.\n\n## Constraints\n- CLI is `dart:io`-bound; this is intentional and complements the web-only DevTools frontend (see .21).\n- Must not duplicate loop logic — every behavior comes from `ExplorationSession`. The CLI only translates flags to config and renders progress.\n- Goal accepted as arg OR stdin (mutually exclusive; arg wins, stdin is the CI-piping path).\n- Output path defaults to `./trajectories/\u003ctimestamp\u003e.jsonl` to keep CI-friendly conventions.\n- Exit code: 0 on session completion, non-zero on harness errors per §17 (`agent_stuck`, `connection_lost`, etc.).\n\n## Ruled out\n- Reimplementing any loop behavior in the CLI layer — rejected; both frontends share .18's driver per PRD §6.2.\n- Auto-launching `flutter run` from the CLI — out of scope; users connect to an existing VM URI.\n- Rich TUI (curses-style) progress — deferred; plain stdout streaming is sufficient for v1 (TTY animations belong in DevTools, not CLI).\n- Bundling AGENTS.md content directly in `--help` text — file path reference is enough; keeps `--help` short.\n\n## Non-obvious notes\n- This bead and .21 split the harness/frontend boundary referenced in PRD §6.2; consult that section for the contract both frontends must honor.\n- AGENTS.md template should reflect the prompt structure from .13 (running summary + per-turn diff + last N actions) so users understand what the agent sees.\n- Depends on .18 (loop driver) and .19 (trajectory writer); both must be feature-complete before this lands.","acceptance_criteria":"- [ ] CLI parses required and optional flags with helpful --help\n- [ ] Goal accepted as arg or via stdin\n- [ ] Per-tier model defaults applied\n- [ ] AGENTS.md template shipped under templates/\n- [ ] End-to-end: 'exploration_cli --goal X --vm-uri Y --model Z' runs against a sample app and writes a trajectory","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:15Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:47:24Z","dependencies":[{"issue_id":"lenny-cx6.20","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:14Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.20","depends_on_id":"lenny-cx6.18","type":"blocks","created_at":"2026-05-07T09:01:32Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.20","depends_on_id":"lenny-cx6.19","type":"blocks","created_at":"2026-05-07T09:01:33Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.19","title":"Trajectory writer (JSONL) with plugin contribution metadata","description":"JSONL writer for the trajectory log. Header: session metadata (goal, AGENTS.md hash, build identifier, model identifier, harness version, list of active plugins with package versions). Per turn: full observation (core + plugin fragments), stability metadata, proposed action, validation result, executed action, diff (core + plugin diffs), summary update. Final record: outcome (done | budget_exhausted | harness_error) + final summary. Web-compatible — DevTools panel writes via DTD filesystem APIs, CLI writes via dart:io. PRD §14, §22.","design":"## Approach\nJSONL trajectory writer with a cross-cutting `TrajectorySink` abstraction so the same writer logic runs in both the CLI frontend (filesystem via `dart:io`) and the DevTools panel (filesystem via Dart Tooling Daemon APIs).\n\n**Architecture (two layers):**\n1. **`TrajectoryWriter`** (in `exploration_agent`, web-compatible). Consumes typed `TurnRecord` / `SessionHeader` / `SessionFooter` values from the loop driver (.18), serializes each as one JSON line, hands the bytes to a `TrajectorySink`. Writer flushes after every turn — a crashed session's trajectory is always recoverable up to and including the last fully-completed turn.\n2. **`TrajectorySink`** (interface, web-compatible). Two impls live outside this package:\n   - **`FileTrajectorySink`** in `exploration_cli` (uses `dart:io` `File`/`IOSink`).\n   - **`DtdTrajectorySink`** in `exploration_devtools` (uses Dart Tooling Daemon's filesystem service via `package:dtd`).\n\nThe harness library only knows about the sink interface — it never imports `dart:io` (PRD §22).\n\n**Record shapes (PRD §14):**\n- **Header** (line 1): `{type: \"header\", goal, agentsMdHash, buildIdentifier, modelIdentifier, harnessVersion, plugins: [{namespace, packageVersion, contractVersion}], config: {budgets, policies}}`. Plugins recorded with **package versions** so a trajectory remains readable months later when a plugin's been upgraded — readers can detect schema mismatches and degrade gracefully.\n- **Per-turn**: `{type: \"turn\", index, observation: {core, plugins}, stability, proposedAction, validation: {result, retries}, executedAction, diff: {core, plugins}, summaryUpdate, modelMetadata: {tokensIn, tokensOut, durationMs}}`.\n- **Plugin-disable event** (zero or more, between turns): `{type: \"plugin_disabled\", namespace, reason, turn}` — captures auto-disables (.18) so timeline UI (.24) can render them.\n- **Footer** (last line): `{type: \"footer\", outcome, finalSummary, totalTurns, totalDurationMs, harnessError?}`. Always written even on `harness_error` — the loop driver (.18) wraps its outermost try/finally around the writer's `close()`.\n\n## Constraints\n- **Web-compatible** per PRD §22: the `TrajectoryWriter` and `TrajectorySink` interface are in `exploration_agent` and **must not import `dart:io`**. The CI guard from .11 covers this.\n- Per-turn flush is non-negotiable — partial trajectories must always be readable. JSONL is naturally append-and-flush friendly.\n- 30-min session must produce \u003c50MB trajectory (PRD §23 success criterion). Screenshots are the bulk; if defaults blow the budget, document compression or down-sampling toggles in the writer config (don't silently drop).\n- Header's plugin-version metadata is mandatory for forward compatibility.\n\n## Ruled out\n- A single concrete writer with `kIsWeb` branching: keeps `dart:io` out of the import graph entirely (kIsWeb branching still imports the symbol). Sink-interface split is cleaner and gets web-compat for free.\n- Streaming format other than JSONL (e.g., proto, msgpack): JSONL is human-greppable, line-tailable for live timeline (.24), and trivial for the CLI viewer. The 50MB target is achievable in JSONL given screenshot-handling.\n- Compressed JSONL (`.jsonl.gz`): trades inspectability for size; defer until there's a real size problem.\n- Letting plugins write their own segments to the trajectory: plugin contributions are already captured in `observation.plugins.\u003cnamespace\u003e` and diff. A plugin-direct write channel would add a new contract surface for marginal value.\n- Buffering across turns to batch writes: defeats per-turn-flush guarantee.\n\n## Non-obvious notes\n- Depends on .11 (lives in the harness lib). Blocks .18 (loop driver writes here), .20 (CLI provides FileTrajectorySink), .21 (DevTools provides DtdTrajectorySink), .24 (timeline panel reads JSONL — also live-tails).\n- The web-compat split is **the** architectural decision in this story — note the trade-off explicitly so reviewers don't push back on the indirection.\n- Plugin contribution metadata in the header lets future tooling (and downstream trajectory consumers) reason about which plugins were active when a turn was captured. Important for the v2 trajectory-replay feature even though we're not building it now.\n- Reading trajectories: the CLI viewer (.20) and DevTools timeline panel (.24) both consume this format — keep the schema documented in `exploration_agent`'s README so independent readers can be written.\n- Cross-references: PRD §14 (trajectory persistence), §22 (harness-in-web-app risk + DTD filesystem mitigation), §23 (30-min/\u003c50MB success criterion).","acceptance_criteria":"- [ ] JSONL has typed header line and per-turn records per PRD §14\n- [ ] Plugin contribution metadata recorded so trajectories remain readable later\n- [ ] Final record always written, even on harness_error\n- [ ] Per-turn flush so a crashed session preserves progress\n- [ ] Web-compatible writer abstraction; CLI uses dart:io, DevTools uses DTD","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:19Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:51:32Z","dependencies":[{"issue_id":"lenny-cx6.19","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:19Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.19","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:32Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":1,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.16","title":"Anthropic and OpenAI providers","description":"Frontier-API providers for Anthropic (Claude 4.6+ class) and OpenAI (GPT-5 class). Wrap their respective SDKs / HTTP APIs behind ModelProvider. Capability metadata: vision per model, no preserve_thinking equivalent. Apply per-tier defaults: lower temperature, smaller observation budgets, tighter retry budgets vs the Qwen3.6 path. PRD §16.4.","design":"## Approach\nTwo concrete `ModelProvider` implementations behind the .14 interface:\n\n**`AnthropicModelProvider`.** Targets Claude 4.6+ class models. Uses the official Anthropic Dart SDK if web-compatible; otherwise `package:http` directly against the Messages API. Tool-use translation: map the harness's tool list to Anthropic's `tools` array, parse `tool_use` content blocks back into the harness's `ModelDecision` shape. Vision via `image` content blocks (base64-encoded PNG when capability reports vision=true). Streaming for the DevTools thinking panel (.23): Anthropic doesn't expose a `\u003cthink\u003e` block by name, so emit `ThinkingDelta` events from the model's text content stream when a `thinking` content block (extended-thinking models) is present, otherwise from the rationale field of the structured output.\n\n**`OpenAiModelProvider`.** Targets GPT-5 class. Uses Responses API (or Chat Completions if Responses isn't available for the chosen model) with structured outputs (strict JSON schema mode) for the dynamic action schema from .14. Vision via `image_url` content parts. Streaming thinking via OpenAI's reasoning summary content where supported.\n\nBoth providers share helpers (lifted into the harness lib) for: schema-strict output enforcement, tool definitions translation, vision-payload attachment, and thinking-delta extraction.\n\n**Per-tier defaults** (PRD §16.4): `temperature=0.3-0.7` (lower than Qwen path), tighter retry budget (1 schema retry, no extra tool-validation retries beyond .17's), smaller observation budgets (4KB total vs 6KB) since frontier-API tokens are billed and large per-turn payloads add up. `preserveThinking=false` (no equivalent to Qwen's mode); capability surface reports this honestly.\n\n## Constraints\n- **Web-compatible** per PRD §22: HTTP-only; pick SDKs that don't transitively import `dart:io`. If the Anthropic/OpenAI Dart SDKs do, fall back to `package:http`.\n- API keys come from session config; never persisted by the provider. Frontend (CLI: env var; DevTools: per-session input) is responsible for sourcing them.\n- Capability metadata must reflect the **specific** model selected (e.g., GPT-5 vision vs older OpenAI text-only): users select model name, capabilities follow.\n\n## Ruled out\n- A single `OpenAiCompatibleProvider` covering both Anthropic and OpenAI: their tool-use shapes are different enough (content blocks vs tool_calls, native vision vs image_url) that splitting is cleaner than abstracting.\n- Implementing prompt caching specific to each provider in this story: we use it (caching the AGENTS.md + system prompt where supported) but the optimization is small and self-contained, not blocking the loop driver.\n- Local routing/fallback between Anthropic and OpenAI inside one provider: the loop driver (.18) selects a single provider per session. Multi-provider fallback is a v2 concern.\n- Custom thinking-trace prompting workaround when the model lacks a thinking content block: just leave the stream empty for that provider; the DevTools panel handles empty streams gracefully.\n\n## Non-obvious notes\n- Depends only on .14 (interface). Blocks .18 — the loop driver depends on at least one concrete provider being available, and these are the providers used as fallback when MLX/Qwen3.6 stalls (PRD §16.4).\n- Anthropic's extended-thinking models are particularly suited for the \"frontier fallback when local stalls\" use case; document this in the provider docs.\n- Cross-references: PRD §16.4 (other model tiers + per-tier defaults), §22 (web-compat for both providers).\n- Use this skill's claude-api skill conventions if available (prompt caching, model selection) when filling out the Anthropic provider — Opus 4.7 is the current default model class; do NOT pin to retired model IDs.","acceptance_criteria":"- [ ] Anthropic provider supports Claude 4.6+ tool-use and vision\n- [ ] OpenAI provider supports GPT-5 class tool-use and vision\n- [ ] Frontier tier defaults applied (temp/budgets/retries)\n- [ ] Both work end-to-end from the harness loop","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:17Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:49:43Z","dependencies":[{"issue_id":"lenny-cx6.16","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:16Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.16","depends_on_id":"lenny-cx6.14","type":"blocks","created_at":"2026-05-07T09:01:25Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.13","title":"Memory artifacts and prompt construction with dynamic merged tool list","description":"Implements the two memory artifacts and assembles the per-turn prompt. Running summary buffer (model-authored, 500-token soft / 1000-token hard cap, rejects oversize updates). Last 3-5 actions buffer with one-line outcomes. Prompt assembler combines AGENTS.md + goal + summary + last N actions + observation + diff. CRUCIALLY: the prompt's tool list is the dynamic merged set (host core + active plugin tools), regenerated each turn so adding/removing plugins changes the action shape. PRD §10 step 5, §13, §16.2.","design":"## Approach\nThree small cooperating components inside `exploration_agent`:\n\n1. **`RunningSummary` buffer.** Model-authored. Token-counted (provider supplies a counter; for MLX/Qwen3.6 this is the tokenizer). Soft cap 500 tokens, hard cap 1000. `update(text)` appends/replaces; oversized updates above the hard cap are rejected with a typed error so the loop driver (.18) can surface a model-readable retry. Soft-cap excess is allowed but flagged in trajectory metadata.\n2. **`ActionRing` buffer.** Fixed-size (default 5) ring of `ExecutedAction` records, each compressed to one-line outcome (`tap(node:42) → ok`, `dio.cancel_in_flight() → cancelled 2`). Verbatim into prompt per PRD §13.\n3. **`PromptAssembler`.** Pure function: `(agentsMd, goal, summary, actions, observation, diff, mergedToolList) → PromptPayload`. Output is a structured payload (system + user messages + tool definitions) the model provider (.14) consumes — not a string. Tool definitions are schema-typed for constrained output composition downstream.\n\n**Load-bearing v0.5 design point:** the merged tool list passed to the assembler is regenerated **each turn** from the host's currently-active plugin set (PRD §10 step 5, §13 final paragraph, §16.2). If a plugin auto-disables mid-session (.18 logic), the very next prompt's tool list reflects that — the model sees a different action shape. The assembler does not cache or memoize the tool list; it asks the session for the current snapshot.\n\n## Constraints\n- **Web-compatible** per PRD §22: pure Dart.\n- Token counting must work in-browser (MLX path uses tokenizer over HTTP for browser; Anthropic/OpenAI providers expose their own counters per .14 capability metadata).\n- Prompt assembly is deterministic given inputs; no hidden timing or randomness — important for trajectory replay (post-v1) and reproducibility.\n\n## Ruled out\n- Caching the merged tool list across turns: directly contradicts PRD §16.2 (\"composed each turn\"). Plugin enable/disable would silently desync.\n- Letting the model author the diff (text summary): per PRD §11.3 / §13 the diff is mechanical and host-authored; the model only authors the running summary.\n- Single-string prompt blob: providers want structured messages + typed tools (§14, §16); building structure now avoids reshaping later.\n- Hard-truncating the running summary on overflow: rejection-with-retry is honest about the budget breach and keeps memory faithful to what the model committed.\n\n## Non-obvious notes\n- Depends on .11 (session owns active plugin list) and .12 (diff is an input). Blocks .18 (loop driver assembles per turn).\n- The \"regenerated each turn from active plugins\" design is the v0.5 plugin-architecture payoff — verify this in tests by enabling/disabling a fake plugin and asserting the next prompt's tool list changes. This is also what .18's auto-disable hangs off of.\n- AGENTS.md is read once at session start (passed in via config from frontend) and included verbatim every turn — the model relies on it as stable orientation.\n- Cross-references: PRD §10 step 5, §13 (memory artifacts), §16.2 (dynamic schema composition), §11.4 (observation budget — informs how aggressively to compress diff in prompt).","acceptance_criteria":"- [ ] Running summary enforces soft and hard token budgets\n- [ ] Last-N actions buffer trims to a fixed window\n- [ ] Prompt assembler produces structured payload with merged tool list\n- [ ] AGENTS.md, diff, stability metadata included verbatim\n- [ ] Removing a plugin changes the next turn's tool list","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:58:14Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:48:08Z","dependencies":[{"issue_id":"lenny-cx6.13","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:58:14Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.13","depends_on_id":"lenny-cx6.11","type":"blocks","created_at":"2026-05-07T09:01:22Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.13","depends_on_id":"lenny-cx6.12","type":"blocks","created_at":"2026-05-07T09:01:22Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.9","title":"Runtime error ring buffer with chained handler API","description":"Bounded ring buffer (default 50) capturing unhandled exceptions, framework asserts, render errors. Hooks FlutterError.onError and PlatformDispatcher.instance.onError exactly once at host init. Plugins that need their own handlers chain through PluginContext.registerErrorHandler — host calls them in registration order, each returns whether it handled the error. Each entry: message, first 5 stack frames, wall-clock offset. Expose with a 'since' cursor for per-turn delta. PRD §6.1, §7.5, §11.","design":"## Approach\nImplement a bounded ring buffer (default 50 entries, configurable) inside `ExplorationBinding` that captures runtime errors from the running app. At host init, hook `FlutterError.onError` and `PlatformDispatcher.instance.onError` exactly once and wire each error event through a chain: the host's ring-buffer recorder PLUS any plugin-registered handlers from `PluginContext.registerErrorHandler` (cx6.3), called in registration order. Each handler returns whether it considers the error \"handled\"; the host respects that signal but still records the error in its own ring buffer regardless (the buffer is observation context, not error suppression). Each entry is `{message, stackFrames[0..5], wallClockOffsetMs}`. Expose `ext.flutter.exploration.core.get_recent_errors?since=\u003ccursor\u003e` returning entries appended after the cursor and the new cursor for the next call.\n\n## Constraints\n- Hook each handler EXACTLY once at host init — re-hooking on hot reload or repeated `ensureInitialized()` calls is a defect.\n- Ring buffer bounded; no unbounded memory growth across long sessions.\n- Stack frames capped at first 5 — full stacks blow the observation budget and aren't differentially useful for the model.\n- The `since` cursor must support per-turn delta retrieval cleanly; cx6.8 calls this each turn to populate `core.errors[]` in the observation.\n- kDebugMode gating (PRD §6.1).\n- Web-compatible wire format (pure JSON).\n\n## Ruled out\n- A separate v0.2-style \"error-chaining decision bead\" / standalone error chaining mechanism: collapsed into `PluginContext.registerErrorHandler` from cx6.3. Plugins that need their own error handling chain through the host's hook (PRD §6.1, §7.5); they do NOT install their own framework-level error handlers, which would conflict with the host's single-install invariant. This is a v0.5 simplification — one chain, one buffer, one VM extension.\n- Persisting errors to disk inside the binding: out of scope. Trajectory persistence (cx6.19) handles durable storage; the binding's job is the live ring buffer.\n- Capturing errors from isolates other than the main one: out of v1. Document the limitation.\n\n## Non-obvious notes\n- The chaining order is registration order from cx6.3's plugin list. The host's own recorder runs first or last (pick one and document) so ring-buffer behavior is deterministic regardless of plugin handler decisions.\n- Plugin handlers may throw — wrap each call in try/catch consistent with cx6.3's per-method exception isolation; a throwing handler contributes a strike toward the 3-strikes auto-disable rule.\n- The \"wallClockOffsetMs\" is relative to session start so the model can correlate errors with actions across the trajectory.\n- This bead is a prerequisite for cx6.8's core fragment (errors block) and is consumed by cx6.18's loop driver (PRD §17 \"Runtime error in app under test. Captured in observation, doesn't end session.\").\n- The \"since\" cursor pattern keeps the host stateless w.r.t. consumer position — the harness owns the cursor.","acceptance_criteria":"- [ ] FlutterError.onError and PlatformDispatcher onError hooked exactly once\n- [ ] Plugin-registered handlers chained in registration order\n- [ ] Ring buffer bounded (default 50, configurable)\n- [ ] Each entry has message + first 5 stack frames + offset\n- [ ] getRecentErrors supports a since cursor for delta","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:11Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:50:40Z","dependencies":[{"issue_id":"lenny-cx6.9","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:10Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.9","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:06Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.9","depends_on_id":"lenny-cx6.3","type":"blocks","created_at":"2026-05-07T09:01:14Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.7","title":"Screenshot capture (host)","description":"Host emits a PNG screenshot of the app surface. Default ON when configured model is vision-capable, default OFF for text-only models — model capability detected through the provider abstraction; user-overridable. Returned as base64 in the observation when enabled. PRD §11.1.","design":"## Approach\nImplement a host VM service extension `ext.flutter.exploration.core.screenshot` that grabs the current rendered surface, encodes it as PNG, and returns it as a base64-encoded string suitable for direct inclusion in the JSON observation. Use `RendererBinding.instance.renderViews` (or `renderView` for single-view apps) to capture via `toImage` and `ImageByteFormat.png`. The default-on flag is determined by the model provider's capability metadata (PRD §11.1, §16.3): vision-capable model → default ON, text-only model → default OFF. The harness (cx6.11/.18) reads this flag and only invokes the screenshot extension when enabled, so the disabled path adds zero cost in the binding.\n\n## Constraints\n- Default ON for vision-capable models (the Qwen3.6-35B-A3B target case and frontier models with vision); default OFF for text-only (PRD §11.1, §16.3). User-overridable via session config.\n- PNG encoding with reasonable compression — screenshots will be carried in the observation budget and sent to the model; oversized PNGs blow the budget.\n- kDebugMode gating (PRD §6.1).\n- Capture must work on every Flutter target (mobile, desktop, web) — the host is platform-agnostic per PRD §3 goals.\n\n## Ruled out\n- JPEG encoding: rejected for v1 — PNG is lossless and the size delta is rarely the binding constraint at typical screen sizes; revisit only if budget pressure shows up empirically.\n- Multi-view tiled screenshots: out of scope for v1; single-view apps are the primary target. Multi-view support deferred.\n- Including the screenshot in the observation budget calculation as a blob: screenshots are sent to the model as a separate input modality (the vision channel), not as part of the JSON observation budget — they don't compete with the 6KB serialized text budget (PRD §11.4).\n\n## Non-obvious notes\n- Screenshot capability detection happens through the provider abstraction (cx6.14), not in this bead — this bead exposes the extension and a configuration flag; the harness wires the flag based on model metadata.\n- For the local Qwen3.6-35B-A3B target, screenshots are first-class input (PRD §16.3 \"Vision-language model, not text-only ... Screenshots are a first-class input modality\"). The semantics tree is still primary action surface; screenshots provide visual disambiguation and fallback context for screens with sparse semantics.\n- Web target: `toImage` works in Flutter web but has CORS implications when the harness runs from a web context (PRD §22 \"Model provider feasibility from web\"). Capturing in-app and returning base64 over VM service avoids the browser-CORS issue entirely.\n- Disabled path performance: the harness simply doesn't call the extension. No flag-checking inside the binding's hot path; the binding always exposes the extension and the harness decides whether to invoke it.","acceptance_criteria":"- [ ] Screenshot extension returns a current-frame PNG\n- [ ] Encoding/decoding round-trips cleanly\n- [ ] Default-enabled flag is wired to model capability metadata\n- [ ] Disabled path adds zero cost to the observation","status":"draft","priority":2,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:09Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:49:42Z","dependencies":[{"issue_id":"lenny-cx6.7","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:08Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.7","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:05Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.7","depends_on_id":"lenny-cx6.2","type":"blocks","created_at":"2026-05-07T09:01:13Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"lenny-cx6","title":"Flutter Exploration Agent","description":"Build a Dart-based agent harness that drives a running Flutter app via a custom WidgetsBinding, organized as a small policy-free host plus a typed plugin contract. Ships v1 with: host binding (exploration_flutter), harness library (exploration_agent), two frontends (CLI and DevTools extension), three reference plugins (Router, Riverpod, Dio), and a plugin authoring guide. Spec: docs/flutter_exploration_agent_prd_v0.5.md.","design":"## Decomposition (PRD v0.5 → v1 stories)\n\n### Pre-build spike\n- lenny-18f (P1, decision): mlx-vlm qwen3_coder tool-call parser — blocks lenny-cx6.15\n\n### Stories (28)\n\n**Phase 0 — Scaffolding**\n- lenny-cx6.1 (P1): Repo \u0026 multi-package scaffolding (7 packages)\n\n**Phase 1 — Host binding (exploration_flutter)**\n- lenny-cx6.2 (P1): ExplorationBinding skeleton, lifecycle, conflict detection, ext namespace\n- lenny-cx6.3 (P1): Plugin contract: types, registry, PluginContext (load-bearing v0.5 artifact; non-breaking versioning posture per §7.7)\n- lenny-cx6.4 (P1): Frame stability signal collection\n- lenny-cx6.5 (P1): Semantics tree capture \u0026 stable IDs (host-owned; custom-widget exposure is a plugin concern in v0.5)\n- lenny-cx6.6 (P1): Core action tools (10 tools in 'core' namespace)\n- lenny-cx6.7 (P2): Screenshot capture\n- lenny-cx6.8 (P1): Stable observation primitive (3 policies + plugin busy-state polling + §11.4 budget split: 4KB core / 2KB plugins / 6KB total)\n- lenny-cx6.9 (P2): Runtime error ring buffer with chained handler API\n- lenny-cx6.10 (P3): Host diagnostic — warn when interactive widgets lack semantics (points users at plugin guide for the fix)\n\n**Phase 2 — Harness library (exploration_agent, web-compatible)**\n- lenny-cx6.11 (P1): Harness scaffolding, VM service connection, ExplorationSession API\n- lenny-cx6.12 (P1): Observation assembly \u0026 per-turn diff (core + plugins)\n- lenny-cx6.13 (P2): Memory artifacts and prompt construction with dynamic merged tool list\n- lenny-cx6.14 (P1): Model provider abstraction with dynamic constrained-output schema\n- lenny-cx6.15 (P1): MLX provider for Qwen3.6-35B-A3B (vision, qwen3_coder, preserve_thinking, sampling defaults) — blocked on spike\n- lenny-cx6.16 (P2): Anthropic + OpenAI providers\n- lenny-cx6.17 (P1): Action validator against merged tool list\n- lenny-cx6.18 (P1): Perception-action loop driver with budgets and failure handling (incl. plugin auto-disable)\n- lenny-cx6.19 (P2): Trajectory writer (JSONL) with plugin contribution metadata\n\n**Phase 3 — CLI frontend**\n- lenny-cx6.20 (P2): CLI frontend (exploration_cli) + AGENTS.md template\n\n**Phase 4 — DevTools extension (exploration_devtools)**\n- lenny-cx6.21 (P2): DevTools extension scaffold + auto-discovery + DTD persistence\n- lenny-cx6.22 (P2): Prompt panel\n- lenny-cx6.23 (P2): Thinking panel (live streaming)\n- lenny-cx6.24 (P2): Timeline panel\n\n**Phase 5 — Reference plugins**\n- lenny-cx6.25 (P2): exploration_router (raw Router/Navigator)\n- lenny-cx6.26 (P2): exploration_riverpod\n- lenny-cx6.27 (P2): exploration_dio (validates busy-state mechanism)\n\n**Phase 6 — Documentation**\n- lenny-cx6.35 (P2): Plugin authoring guide — depends only on .3 (contract); reference-plugin worked examples slot in as plugins land\n\n### Follow-ups (deferred / not v1-critical)\n- lenny-cx6.29 (P3): Sample non-trivial app for dogfooding\n- lenny-cx6.30 (P3, deferred 45d): Empirical 800ms tuning\n- lenny-cx6.31 (P3): DevTools mid-turn interrupt (decide cheap vs v2 during panel work)\n- lenny-cx6.32 (P3, deferred 60d): Plugin scaffolding generator\n- lenny-cx6.33 (P3, deferred 90d): Marionette compatibility plugin (post-v1)\n- lenny-cx6.34 (P3, deferred 180d): Plugin contract extension for resources \u0026 prompts (v2)\n\n### Build order\nL0: .1\nL1: .2 (parallel: spike, .29 sample app)\nL2: .3, .4, .5, .7  (parallel host modules)\nL3: .6 (after .5), .9 (after .3), .10 (after .5), .11 (after .2,.3), .35 (after .3 — plugin guide drafts in parallel)\nL4: .8 (after .3,.4,.5,.7,.9), .14 (after .11), .19 (after .11)\nL5: .12 (after .8,.11), .15 (after .14, spike), .16 (after .14), .25/.26/.27 (after .3,.11)\nL6: .13 (after .11,.12), .17 (after .3,.11,.12)\nL7: .18 (after .6,.13,.16,.17,.19) — convergence\nL8: .20 (after .18,.19), .21 (after .11,.18,.19)\nL9: .22, .23, .24 (after .21; .24 also after .19)\n\n### Open questions captured but not blocking decomposition\n- 800ms stability default → empirical task lenny-cx6.30\n- Tool-selection accuracy at long context → empirical, validate during integration\n- Hard plugin dependencies → default no, revisit if friction\n- Plugin-owned identifier convention → captured in plugin guide (.35); convention is 'fragment includes IDs accepted by tools'\n- DevTools mid-turn interrupt → lenny-cx6.31, decide during panel work\n\n### v2 / explicitly out-of-v1\n- Adversarial exploration (§15.3)\n- Trajectory replay under integration_test (§15.4)\n- Multi-device parallel sessions\n- DevTools panel features beyond the 3 v1 sub-panels\n- Trajectory-based fine-tuning\n- Custom gestures\n- Demonstration recording\n- Hot-reload-aware sessions\n- Auto-discovery from pubspec.yaml\n- Marionette compatibility (lenny-cx6.33)\n- Plugin contract extension for resources/prompts (lenny-cx6.34)","acceptance_criteria":"- [ ] User can install host, configure 1-3 plugins, run a session on a representative app in \u003c15 minutes from install\n- [ ] Qwen3.6-35B-A3B at 8-bit completes 'login → settings → change Riverpod-managed setting → logout' on a representative app in \u003c20 turns ≥80% of attempts\n- [ ] action-relative stability terminates within budget on ≥95% of post-action turns, including correctly waiting for dio in-flight requests\n- [ ] A third party can write a working plugin from the authoring guide alone (no host source needed)\n- [ ] Reference plugins compose with each other and with apps using only some of Router/Navigator, Riverpod, Dio\n- [ ] 30-min session produces \u003c50MB trajectory reviewable both in CLI viewer and DevTools timeline panel\n- [ ] DevTools panel auto-discovers when app depends on exploration_flutter and presents a usable loop within 30 seconds\n- [ ] Thinking-trace stream keeps up with model generation rate on M3 Ultra (no perceptible lag)","status":"open","priority":2,"issue_type":"epic","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:55:57Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:31:43Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.34","title":"Plugin contract extension: resources \u0026 prompts (v2)","description":"PRD §8: defer Resources (passive content the agent reads on demand) and Prompts (pre-authored multi-step workflows) to v2. The plugin contract should leave room: extension/exploration/config.yaml accommodates resources:/prompts: keys, ExplorationPlugin can grow getters in a backward-compatible way. Implement when (a) chat UI is on the roadmap or (b) a plugin author needs reference material exceeding the per-turn observation budget. Out of v1 scope.","acceptance_criteria":"- [ ] ExplorationResource and ExplorationPrompt types defined\n- [ ] Manifest schema extended\n- [ ] Backward-compatible with v1 plugins\n- [ ] Reuses MCP/Packaged-AI-Assets-shaped source files where possible","status":"deferred","priority":3,"issue_type":"task","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T14:00:04Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:00:04Z","defer_until":"2026-11-03T09:00:04Z","dependencies":[{"issue_id":"lenny-cx6.34","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T09:00:03Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.33","title":"Marionette compatibility plugin (post-v1)","description":"PRD §20.1: a MarionetteCompatibilityPlugin exposing Marionette's familiar tool set through the host. Three approaches: inheritance (preferred — ExplorationBinding extends MarionetteBinding, contingent on Marionette's class being open to subclassing), reimplementation (bounded work, loses free updates), or coexistence (not viable). Verify Marionette source for subclass-friendliness; if brittle, fall back to reimplementation. Explicitly post-v1 per PRD §18.","acceptance_criteria":"- [ ] Marionette source reviewed for subclass-friendliness\n- [ ] Path chosen (inheritance vs reimplementation) and documented\n- [ ] Plugin ships exposing the §20.1 tool set","status":"deferred","priority":3,"issue_type":"task","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T14:00:02Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:00:03Z","defer_until":"2026-08-05T09:00:03Z","dependencies":[{"issue_id":"lenny-cx6.33","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T09:00:02Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.32","title":"Plugin scaffolding generator (exploration_plugin create)","description":"PRD §19 mentions a 'template package generator (exploration_plugin create my_plugin)'. Generates a new plugin package with the contract scaffolding, manifest, and minimal observe()/busyState() stubs. Nice-to-have, not v1-blocking — written manually until generator exists. Defer until contract has stabilized.","acceptance_criteria":"- [ ] Generator produces a working plugin skeleton from a single command\n- [ ] Generated package compiles and registers cleanly\n- [ ] Generator covers config.yaml manifest","status":"deferred","priority":3,"issue_type":"task","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T14:00:01Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:00:01Z","defer_until":"2026-07-06T09:00:01Z","dependencies":[{"issue_id":"lenny-cx6.32","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T09:00:01Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.31","title":"DevTools panel: mid-turn interrupt support","description":"PRD §21 open question: should the DevTools panel allow the user to interrupt the agent mid-turn (cancel a long generation, inject a hint into the running summary)? Default position: yes, both, but only if cheap on top of existing session state. If it adds meaningful complexity, defer to v2. Defaults to v2 unless trivial during S22-S24 implementation.","acceptance_criteria":"- [ ] Decide cheap vs meaningful complexity\n- [ ] If cheap: implement cancel + hint injection\n- [ ] If meaningful: document as v2 explicitly","status":"open","priority":3,"issue_type":"task","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T14:00:00Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:00:00Z","dependencies":[{"issue_id":"lenny-cx6.31","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T09:00:00Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.31","depends_on_id":"lenny-cx6.22","type":"blocks","created_at":"2026-05-07T09:02:54Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.31","depends_on_id":"lenny-cx6.23","type":"blocks","created_at":"2026-05-07T09:02:55Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.30","title":"Empirical tuning of action-relative stability default (800ms)","description":"PRD §21 open question: the 800ms default still needs empirical tuning against real apps. Run sweeps against the sample app + 1-2 real apps measuring observation-completeness vs latency tradeoff. Adjust default if warranted. Not v1-critical — can ship with the 800ms guess.","acceptance_criteria":"- [ ] Tuning sweep run against ≥3 apps\n- [ ] Default value justified or revised\n- [ ] Findings documented in PRD addendum","status":"deferred","priority":3,"issue_type":"task","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:59Z","created_by":"Nico Spencer","updated_at":"2026-05-07T13:59:59Z","defer_until":"2026-06-21T08:59:59Z","dependencies":[{"issue_id":"lenny-cx6.30","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:58Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.29","title":"Sample non-trivial Flutter app for dogfooding \u0026 success criteria","description":"PRD §23 success criteria reference 'a representative non-trivial app' (login → settings → change Riverpod-managed setting → logout) and 'a 30-minute exploration session against a real running app'. Without a controlled sample app we cannot evaluate the success metrics. Should use Navigator-managed routes AND Riverpod state AND Dio networking so all three reference plugins are exercised end-to-end. Either build under examples/ or pick an OSS Flutter app.","acceptance_criteria":"- [ ] Sample app supports login, settings change, logout\n- [ ] Uses Navigator routes, Riverpod state, Dio networking\n- [ ] Documented setup steps for ExplorationBinding + reference plugins\n- [ ] Used as fixture in S10 / S25-27 validation","status":"open","priority":3,"issue_type":"task","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:59:58Z","created_by":"Nico Spencer","updated_at":"2026-05-07T13:59:58Z","dependencies":[{"issue_id":"lenny-cx6.29","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:59:57Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"lenny-cx6.10","title":"Host diagnostic: warn when interactive widgets lack semantics","description":"Connect-time host diagnostic only. Scan for interactable-looking widgets (GestureDetector, InkWell, custom buttons) that lack semantics annotations and surface a clear warning. The fix in v0.5 is to write a custom-widget plugin (the host no longer owns isInteractiveWidget knobs); the diagnostic message points the user at the plugin authoring guide. P3 because v0.5 deemphasizes host-side custom-widget handling — the plugin path is the canonical answer.","design":"## Approach\nImplement a connect-time host diagnostic that walks the widget tree (or piggybacks on the semantics walk from cx6.5) to identify common interactable widgets that lack semantics annotations: bare `GestureDetector` (without enclosing `Semantics` or a button-like ancestor), `InkWell` without semantic labels, `MouseRegion` with click handlers but no semantics, and any user-flagged custom-button class via a configurable list. Surface results as a list of `{widget_type, location, suggested_fix_pointer}` records exposed via `ext.flutter.exploration.core.diagnostics_warnings`, called once on harness connect (cx6.11). The diagnostic message text MUST point the user at the plugin authoring guide (cx6.35) as the canonical fix path: \"These widgets won't be visible to the agent. Add semantics annotations, or write a plugin that exposes them — see \u003cplugin guide\u003e.\"\n\n## Constraints\n- Connect-time diagnostic ONLY. Not a runtime concern; not a per-turn observation contributor.\n- Must NOT crash or block the session if the walk fails — degrade to \"no diagnostic this run\" with a log line.\n- P3 priority by design — v0.5 explicitly demoted host-side custom-widget handling. The diagnostic is a developer-experience nicety, not a correctness invariant.\n- kDebugMode gating (PRD §6.1).\n\n## Ruled out\n- The v0.2 `isInteractiveWidget` host configuration knob: explicitly OUT in v0.5. Per PRD §6.1 the host does not own custom-design-system knobs — that's a plugin concern. v0.5 collapses the entire \"host knows about your custom widgets\" path into \"warn the developer, point at the plugin authoring guide, write a plugin.\" This bead is the demotion: the host's only role is to surface the warning and direct the fix to the plugin layer.\n- An auto-fix or auto-annotation feature: rejected — modifying the user's widget tree at runtime to inject semantics is invasive and brittle. The fix path is documentation + plugin.\n- Per-turn diagnostic re-runs: rejected — the widget tree's interactable surface doesn't change meaningfully turn-to-turn at the granularity this diagnostic operates on, and per-turn cost would be wasted.\n\n## Non-obvious notes\n- This bead is the v0.5 corrective for a v0.2 design mistake (the host owning custom-widget detection). The diagnostic message wording is load-bearing: it tells the developer the canonical fix is a plugin, which is consistent with the rest of v0.5's posture (PRD §6.1, §6.4 \"It distributes ecosystem work\").\n- The plugin-guide pointer is to cx6.35's deliverable; until cx6.35 lands, link to a placeholder URL and update post-cx6.35.\n- A sample-app fixture demonstrating both a hit (interactive widget without semantics) and a clean run is part of acceptance — this also serves as a doctest for the cx6.35 guide's \"wrap your custom widgets\" example.\n- Depends on cx6.5's semantics walk to know which widgets DO have semantics; the diagnostic is the negation of cx6.5's filter.","acceptance_criteria":"- [ ] Diagnostic identifies common interactable widgets without semantics annotations\n- [ ] Surfaces results to the harness on connect via dedicated extension\n- [ ] Diagnostic message points at the plugin authoring guide as the fix\n- [ ] Sample-app fixture demonstrates a hit and a clean run","status":"draft","priority":3,"issue_type":"feature","owner":"nicholas.scott.spencer@gmail.com","created_at":"2026-05-07T13:57:11Z","created_by":"Nico Spencer","updated_at":"2026-05-07T14:51:08Z","dependencies":[{"issue_id":"lenny-cx6.10","depends_on_id":"lenny-cx6","type":"parent-child","created_at":"2026-05-07T08:57:11Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.10","depends_on_id":"lenny-cx6.1","type":"blocks","created_at":"2026-05-07T09:01:07Z","created_by":"Nico Spencer","metadata":"{}"},{"issue_id":"lenny-cx6.10","depends_on_id":"lenny-cx6.5","type":"blocks","created_at":"2026-05-07T09:01:18Z","created_by":"Nico Spencer","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"memory","key":"when-user-says-autonomous-discovery-or-autonomous-decomp","value":"When user says 'autonomous discovery' or 'autonomous decomp' or asks for decomposition done on my own: do the bulk of exploration and story decomposition without asking clarifying questions. Read the source PRD/design doc, propose seams, create child stories, wire dependencies, and set priorities autonomously. Leave any unresolved design questions as blocking decision beads (type=decision) or as 'human' flags (bd human \u003cid\u003e) on affected stories — do not stall the decomposition waiting for answers. The user reviews the decomposition at the end, not the questions along the way."}
