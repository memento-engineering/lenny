{"version":1,"anchors":{"version":1,"rubrics":{"spec-validation":"# spec-validation [GATING]\n\nThe hard gate of the spec-readiness committee. This lane does not weigh opinion\n— it is a DETERMINISTIC structural check over the spec the bead carries (the\n`spec-validation` capability runs it in Dart; no model, no judgement). It is\nthe answer to \"a bead must never reach the build stage without an\nimplementation-ready spec\": the specify stage writes the spec INTO the bead,\nand this lane verifies the structure landed whole.\n\n## What it checks\n\n- **Acceptance criteria** — at least one testable `- [ ]` checkbox criterion in\n  the bead's acceptance field.\n- **The design carries all four sections** — `## Implementation Plan`,\n  `## Touches`, `## ADR Alignment`, and `## Validation Plan` (with at least one\n  `- ` item).\n- **`## Implementation Plan` carries ORDINAL-LED steps.** Every step opens with\n  an ordinal: an ordered-list item (`1. …` / `1) …`) or an ordinal heading\n  (`### Step 1 — …` / `### 1. …` / `**Step 1:** …`). Both are ordered and\n  addressable; a bulleted or prose-only plan is neither and fails, however\n  complete it is. The check reads the `## Implementation Plan` section's own\n  body — an ordinal in another section does not stand in for a step.\n- **No placeholder token in PROSE** — `TBD`, `TODO`, `implement later`,\n  `fill in later`, `as needed`, `appropriate error handling`,\n  `similar to step`. A spec that defers its own content is not\n  implementation-ready.\n- Structure and tokens are read from PROSE alike: markdown QUOTATION contexts\n  (fenced code blocks, `inline code` spans, `>` blockquote lines) are stripped\n  before matching. A spec that QUOTES a token as evidence — a `// TODO` comment\n  its plan deletes, an ADR clause cited verbatim — is pointing at work, not\n  deferring it; and a heading that exists only inside a code block is evidence,\n  not a section.\n\nThe contract is not this lane's secret: `kSpecStructuralContract` states it\nverbatim in the specify agent's own brief, and the exemplar the brief ships is\nround-tripped through this check in test. A spec F'd here failed a rule it was\ntold.\n\n## Bands\n\n- **A** — every structural element is present and placeholder-free. The spec is\n  structurally whole; whether it is also COHERENT, ALIGNED, TESTABLE, and\n  COMPLETE is the four judgement lanes' call, not this one's.\n- **F** — any element missing, any placeholder present, or no spec at all (the\n  pre-specify state — a bead whose specify stage never wrote, or was skipped,\n  never silently passes).\n\nA grade of **F** here is a hard block: the route parks the bead at a gate\n(`gated`) for spec rework. There is no partial credit and no LLM judgement in\nthis lane — a structural element is either present or it is not.","coherence":"# coherence\n\nDoes the spec describe a change that lands as a coherent whole — carving scope\ncleanly from its neighbour beads AND extending the code that already exists,\nrather than duplicating concepts the codebase already owns? Grade the SPEC\n(the bead's acceptance + design), not any code; the bead has not been built\nyet. You are blind to the other lanes' concerns (testability, plan detail, ADR\ncitations) — weigh ONLY fit.\n\nCoherence spans two axes, graded together; the WORSE axis sets the floor:\n\n1. **The bead graph.** A bead is rarely alone: it may have a parent epic,\n   sibling beads, and children. A coherent spec carves scope from its siblings\n   (each named by id, with an explicit \"out of scope here\" line where surfaces\n   could collide), engages the parent's design where one exists, and never\n   proposes work a sibling already shipped. Verify with the bd CLI —\n   `bd dep list <id>`, `bd show <sibling-id>`, `bd search <keyword>` — a\n   claimed relationship that does not match the database is a finding.\n\n2. **The existing codebase.** Every step that adds a type, function, or\n   subsystem should answer: does something equivalent already live in this\n   repo? A coherent spec extends what is there; an incoherent one\n   parallel-builds beside it. This is conceptual duplication, not just literal\n   file duplication — a second verdict-transport parser beside the committee's\n   existing file→stray→envelope stack, a second rubric loader beside\n   `PackagedAssetLoader`, a second git-runner seam beside the registry's\n   shared one. Even clean Dart, in isolation, is incoherent if it re-expresses\n   an abstraction the packs already own. Verify with grep over the worktree:\n   a new symbol the spec introduces that `grep -rn \"<symbol>\" . --include='*.dart'`\n   shows already defined — and the spec does not acknowledge — is a finding.\n\nHouse terminology is part of fit: the seam word is **extension**, never\n\"plugin\"; package and asset names are human faculties/crafts, never\nagent-nouns. A spec that names its new surface against those rules does not\nland coherently.\n\n## Bands\n\n- **A** — both axes covered: scope explicitly carved from every plausibly\n  overlapping sibling (by id), the parent's design engaged where one exists;\n  existing code cited by exact path/symbol with the relationship stated\n  (\"extends\", \"wraps\", \"leaves alone because…\"). A reader can tell from the\n  spec alone where this bead ends and both neighbours begin.\n- **B** — both axes engaged with one small omission that reads as economy, not\n  blindness: an obviously non-colliding sibling unmentioned, or a genuinely\n  new helper added without a duplication disclaimer.\n- **C** — a neighbour acknowledged but not used to constrain the spec; or a\n  small, self-contained duplication (a private helper an existing package\n  already provides) a quick grep would have caught. The fix is\n  renaming/rerouting, not a redesign.\n- **D** — the spec proceeds as if the bead were standalone on one axis: an\n  existing parent/sibling ignored where surfaces overlap, or a parallel\n  subsystem proposed at non-trivial scale (a new package, pipeline, or\n  committee-like layer) without cross-referencing the existing one. The fix is\n  a redesign that routes through what exists.\n- **F** — a load-bearing collision: the spec duplicates work a closed sibling\n  already shipped, contradicts its parent's design on a load-bearing point, or\n  reinvents an existing subsystem from scratch (a second route matrix, a\n  second asset loader). The author did not look, or looked and chose not to\n  acknowledge.\n\n## Calibration\n\n- Acknowledged-and-rejected is NOT a failure: a deliberate parallel\n  implementation is coherent IF the spec contrasts the two and says why the\n  existing one is not reused. Ignored is F; acknowledged-and-justified can be\n  A.\n- A bead with no parent and no siblings grades on the codebase axis only —\n  never penalise a truly standalone bead for not citing nonexistent\n  neighbours.\n- Count concepts, not files: many files expressing one concept is fine; one\n  file carrying two unrelated concerns is the seam that caps the grade.\n- Out of scope: taste (naming style beyond the house rules, idiom, error\n  wording). Grade only graph fit and codebase duplication.","adr-alignment":"# adr-alignment\n\nDoes the spec respect the substation's recorded decisions? Every substation\nkeeps its register under `docs/adr/` — ratified ADRs plus `ADR-0000`, the\nliving AI-decision register whose `A<n>` amendments record autonomous\ndecisions (pending until promoted, but binding on new work unless explicitly\ncontradicted by a human ruling). A spec that touches a surface those decisions\ngovern must cite the relevant decision and either implement it, extend it, or\nexplicitly propose overriding it. A spec that silently contradicts a recorded\ndecision is the most expensive failure this committee can miss: it undoes\ndeliberated work, usually unnoticed until the contradiction ships.\n\nYou are blind to the other lanes' concerns (fit, testability, plan detail) —\nweigh ONLY decision alignment.\n\n## Before grading: search for precedent\n\nEnumerate the decisions that COULD apply before judging novelty. Extract 3-6\nkeywords from the bead's title + touched surfaces, then run, from the worktree\nroot:\n\n```\nls docs/adr/\ngrep -li \"<keyword1>\\|<keyword2>\\|<keyword3>\" docs/adr/*.md\n```\n\nRead every hit — including ADR-0000's amendments, which often carry the\nplacement/naming/seam rulings that bind a spec most directly. Record the\nkeywords you used in your rationale (e.g. \"verified via grep on `committee`,\n`rubric`, `gate` — A9 applies\"), so the claim \"no decision applies\" is itself\nverifiable.\n\n## Bands\n\n- **A** — the spec's `## ADR Alignment` section cites every load-bearing\n  decision by file path AND clause (an ADR number or an `A<n>` amendment id)\n  with a one-sentence quote of the constraining clause, and states how the\n  plan aligns (or how it extends the decision, by name). Where no decision\n  applies, the section says so explicitly with the grep keywords that verified\n  it.\n- **B** — the relevant decision is cited, but without the load-bearing clause:\n  a reader can tell WHICH decision is implemented but not which clause\n  constrains the work.\n- **C** — the spec is clearly downstream of a recorded decision (it extends a\n  surface the decision established) but never cites it; the alignment is\n  accidental rather than acknowledged. Or: the section claims \"no ADR applies\"\n  without naming the keywords that verified it.\n- **D** — your grep returns a decision that plainly governs a surface this\n  spec touches, and the spec does not cite it. The fix is mechanical — add the\n  citation with the quoted clause and apply (or explicitly distinguish) the\n  precedent — so this routes to spec rework, not a human.\n- **F** — a structural contradiction: the spec, AS WRITTEN, would invert or\n  undo a load-bearing clause of a recorded decision, and no citation paragraph\n  can fix it — implementing the spec means the precedent no longer holds. The\n  route parks the bead at a gate; revisiting a precedent is a human call\n  (supersede the decision, or rework the spec to align).\n\n## Calibration\n\n- ADR-0000's PENDING amendments bind for grading purposes: they are the\n  register's whole point (an autonomous decision awaiting promotion is still\n  the recorded state of the world). A spec free to contradict one must SAY it\n  proposes overriding a pending amendment — that names the conflict for the\n  human instead of hiding it.\n- Do not demand citations for decisions that genuinely do not touch the spec's\n  surfaces — a padded ADR section citing everything is noise, not alignment.\n  The A-grade signal is the LOAD-BEARING citation, quoted.\n- The org invariants (the seam word is **extension**, never \"plugin\"; package\n  names are human faculties, never agent-nouns; bd CLI only, never SQL) count\n  as recorded decisions even when a local ADR does not restate them.","acceptance-testability":"# acceptance-testability\n\nAre the bead's acceptance criteria TESTABLE — and is the validation plan a\n1:1, runnable map onto them? Grade the acceptance field and the\n`## Validation Plan` section together, nothing else. You are blind to the\nother lanes' concerns (fit, ADR citations, plan detail) — weigh ONLY whether\n\"done\" is checkable by command.\n\nA criterion is testable when a named command can falsify it: a specific\n`dart test` invocation, a `dart analyze` run, a grep with an expected hit (or\nits absence), a CLI run with expected output. \"Works well\", \"is fast\", \"is\nrobust\" are not criteria — nothing can fail them. The validation plan then\ncarries, for EACH criterion, the exact command and its expected result — the\ndownstream build is validated by running exactly these, so a gap here is a\ncriterion that will never be checked.\n\n## Bands\n\n- **A** — every criterion is an independently testable checkbox, error/edge\n  cases included, ordered most-critical first; the validation plan maps 1:1\n  onto them (every criterion has its command + expected output, every item\n  names its criterion); the commands are real for THIS tree (the named test\n  files/paths exist or are created by the plan; the runner is the house one —\n  `dart test`, `dart analyze`, or an exact shell line).\n- **B** — the criteria are testable and the mapping is whole, with minor\n  looseness: an expected output left implicit where the command makes it\n  obvious, or one edge case folded into a broader criterion.\n- **C** — the happy path is testable but failure modes are not, or the\n  validation plan covers most-but-not-all criteria; the builder must invent\n  checks for the gaps.\n- **D** — criteria mix testable and untestable statements, or the validation\n  plan is generic (\"run the tests\") rather than mapped; two builders would\n  check different things and both could claim done.\n- **F** — the criteria are goals, not checks (\"committee works end to end\"),\n  or there are no checkboxes at all, or the validation plan is absent/asserts\n  nothing a command can falsify. Nothing here can gate a wrong build.\n\n## Calibration\n\n- Actually verify one command per criterion block against the worktree: a\n  validation item naming `test/<file>_test.dart` that neither exists nor is\n  created by the implementation plan is a finding, not economy.\n- A manual verification step is acceptable ONLY where no automated check can\n  exist (a live external service), and must still name the exact action and\n  expected observation. Prefer-automated is the house rule (pure logic tested\n  before IO is wired).\n- Vacuous checks grade no better than none: a validation item that passes even\n  if the change were broken (an unconditional `dart analyze` on an untouched\n  package) does not validate its criterion — treat the criterion as unmapped.\n- Count the mapping BOTH ways: an unmapped criterion caps at C; a validation\n  item mapped to no criterion suggests the criteria are incomplete — name it.","plan-completeness":"# plan-completeness\n\nIs the `## Implementation Plan` literal and decision-free — could a builder\nwith zero context follow it without redoing discovery or making design\nchoices? Grade the plan's words on the page, nothing else. You are blind to\nthe other lanes' concerns (fit, testability, ADR citations) — weigh ONLY\nwhether the plan is complete.\n\nCompleteness has two faces, graded together:\n\n1. **Literalness.** Every step names an exact file path from the repo root, a\n   real Dart code block (the code to write, not a description of it — honoring\n   the memento house set: freezed sealed unions consumed with exhaustive\n   `switch`, Fakes not mocks, doc comments on public API, no `print` in lib\n   code), the exact test command with expected output (`dart test\n   test/<file>_test.dart` → expect PASS), and a conventional-commit message.\n   Shorthand that is unambiguous in context (\"and the symmetric case for X\")\n   is economy; \"wire it up appropriately\" is vagueness.\n\n2. **Decisions made.** A judgment call is any step where the builder must\n   compare alternatives and pick one — which seam to extend, which error\n   posture, which signature. The plan should have made them: where multiple\n   designs were possible, it picked one and named it (ideally with the one-line\n   why). \"Design the shape of X\" inside an implementation plan means the spec\n   stage did not finish its job.\n\nLength buys nothing: a two-step plan quoting exact before/after strings is\ncomplete; a two-hundred-line plan paraphrasing goals is not.\n\n## Bands\n\n- **A** — every step carries all four elements and zero judgment calls; a\n  reader can predict the diff before opening an editor. Named symbols resolve\n  in the current tree (verify with `grep -rn \"<symbol>\" . --include='*.dart'`)\n  or are explicitly announced as new — literal-SOUNDING is not\n  literally-correct.\n- **B** — most steps literal; a handful of unambiguous shorthands, or one or\n  two acknowledged judgment calls with stated reasoning (\"inline, because\n  there is one caller\").\n- **C** — roughly half the steps force inference: the plan says WHAT but the\n  builder finds WHERE, or names the file but leaves the signature open; or a\n  named symbol does not resolve and is not announced as new.\n- **D** — the plan reads as an outline: steps are paragraphs the builder must\n  turn into code, judgment calls recur without resolution, and independent\n  discovery must precede the work. Two competent builders would produce\n  noticeably different diffs.\n- **F** — the plan describes the goal, not the work (\"make the gate\n  stricter\", \"improve the committee's prompts\", \"add tests for this area\");\n  or steps carry no code blocks at all. The builder would have to produce the\n  plan the spec was supposed to provide.\n\n## Calibration\n\n- Before grading A or B, run grep for at least one named symbol per step\n  block; awarding A on faith is the leniency failure this rubric exists to\n  prevent.\n- Naming a test command without its expected output means the builder still\n  decides what passing looks like — more than half the steps like that caps\n  at C.\n- Following a WRITTEN convention (the house lints, conventional commits, bd\n  actor rules) is lookup, not a judgment call — do not penalise a plan for not\n  restating them.\n- The convergence test is the bar: this station's acceptance for a spec is\n  that two independent builds of it converge on the same change. If you can\n  name a step where two builders would plausibly diverge, name it in your\n  rationale and grade accordingly."},"anchors":[{"anchor":"bin/leonard_native_host.dart","resolved":false,"neighbors":[]}],"symbols":["UiAutomator2Backend","AppiumBackend","NativeBackend","NativeSnapshot","ASWebAuthenticationSession"],"priorArt":[],"priorArtWired":false},"context":[{"note":"NativeBackend seam is open and ready for Android impl: lib/src/native_backend.dart documents platform-specific behavior (iOS Done vs Android back keyboard dismiss, attribute/value vs attribute/text readback, iOS consent_accept/alert_dismiss vs Android back key). The seam expects backend impl to handle all divergences internally — not in extension/tools.","source":"lib/src/native_backend.dart:94-104"},{"note":"AppiumBackend is iOS-only production implementation (~600 LOC): HTTP W3C WebDriver transport, XCUITest XML parsing, iOS-specific press key handling (consent_accept→POST /alert/accept, alert_dismiss→POST /alert/dismiss), iOS keyboard dismiss (Done button or no-op for iOS26). Pattern provides reference for UiAutomator2 XML parsing and Android keyboard dismiss (POST /back).","source":"lib/src/appium_backend.dart (full file) + docs/design/leonard-native-appium/backend_skeleton.dart"},{"note":"NativeSnapshot.platform field already supports both 'ios' and 'android' — no schema changes needed. FakeNativeBackend recognizes all keys (enter/return/done/consent_accept/alert_dismiss/back) so unit tests can exercise Android press behavior today.","source":"lib/src/native_snapshot.dart:100-101, lib/src/fake_native_backend.dart:170-180"},{"note":"CLI bin/leonard_native_host.dart exists (not created yet) and already accepts --platform flag, but always instantiates AppiumBackend regardless of platform value. Architect must wire platform-conditional backend selection (e.g., `if (platform == 'android') UiAutomator2Backend(...) else AppiumBackend(...)`)","source":"bin/leonard_native_host.dart:43-48"},{"note":"Public API exports from lib/leonard_native.dart: AppiumBackend is exported, UiAutomator2Backend must be added post-implementation.","source":"lib/leonard_native.dart:14"},{"note":"AppiumBackend documents hardening applied per m2-spec §5.5: B5 (HTTP status + non-JSON handling), FN3 (masked flag element-type-derived), FN4 (attribute/value vs attribute/text branching), B6 (Android keyboard dismiss non-fatal). UiAutomator2Backend must apply same pattern for Android.","source":"lib/src/appium_backend.dart:11-15"},{"note":"Test patterns: appium_backend_test.dart uses MockClient for unit tests (no device); native_host_e2e_test.dart probes Appium reachability, self-skips on env vars. Android e2e will need LEONARD_NATIVE_APP_PACKAGE (instead of LEONARD_NATIVE_APP), LEONARD_NATIVE_APP_ACTIVITY, and Android emulator UDID.","source":"test/appium_backend_test.dart + test/native_host_e2e_test.dart:75-85"},{"note":"Auth0 consent path: iOS has ASWebAuthenticationSession (separate SpringBoard alert, press('consent_accept')→POST /alert/accept). Android has Chrome Custom Tab (no separate system alert). Bead notes adaptive-consent path from lenny-qxx.5 should tolerate Chrome Custom Tab's absence — verify on live run.","source":"lib/src/native_backend.dart:144-150 + native_backend.dart doc comment line 96"},{"note":"Perception is pull-free (ADR-0006, in genesis not lenny): buildPerception() is synchronous, device latency lives behind NativeBackend seam. Out-of-band watcher (NativeExtension.initialize subscribes to backend.watch()) keeps NativeSnapshot current. No async I/O at observation time.","source":"CLAUDE.md:130-149, lib/src/native_extension.dart:47-57"},{"note":"House conventions (memento house set): Dart ^3.11, melos, exhaustive switch, fakes not mocks, doc comments on public API, no print in lib, shared lints (strict-casts/inference/raw-types, prefer_single_quotes, unawaited_futures, avoid_print). Package uses xml (XCUITest parse) + http (W3C WebDriver); Android parser may differ.","source":"CLAUDE.md (referenced)"},{"note":"NativeBackend seam is explicitly designed for additive platform backends. Comments in native_backend.dart state: 'a later `UiAutomator2Backend` is purely additive'","source":"packages/leonard_native/lib/src/native_backend.dart:6"},{"note":"Platform field already exists in NativeSnapshot and accepts 'ios'|'android' per the type comment. Host CLI already wires --platform flag (defaults to 'ios').","source":"packages/leonard_native/lib/src/native_snapshot.dart:96-101, bin/leonard_native_host.dart:38,45"},{"note":"Android-specific divergences are already anticipated in comments: iOS ASWebAuthenticationSession consent vs no system alert on Android; iOS Done vs Android back keyboard dismiss; iOS attribute/value vs Android attribute/text readback. These are scoped as 'inside the impl, never in extension/tools'.","source":"packages/leonard_native/lib/src/native_backend.dart:97-103, lib/src/appium_backend.dart:14-19"},{"note":"The m5 spec (Auth0 e2e, iOS-proven milestone) notes Android as explicitly out-of-scope but mentions 'the adaptive-consent path from lenny-qxx.5 should already tolerate its absence' for Android (where no system consent alert appears).","source":"docs/design/leonard-native-appium/m5-spec.md:34, 97"},{"note":"The m2 spec explicitly defers Android as 'purely additive later'. Per-platform branching is kept behind the NativeBackend seam interface.","source":"docs/design/leonard-native-appium/m2-spec.md:3, 24"},{"note":"Chrome Custom Tab is noted in the bead description as the Auth0 Android flow (distinct from iOS ASWebAuthenticationSession), but the bead's acceptance criteria focus on UiAutomator2 backend impl + unit tests + live Android e2e, not Auth0-specific adaptive handling.","source":"lenny-qxx.7 bead task description"},{"note":"Ratified standards: ADR-0001 (Perception framework, Accepted design), ADR-0002 (Perception migration, Accepted strategy), ADR-0003 (Model backend on dartantic, Accepted decision, 2026-06-16). All are about perception/model layers, not platform backends. No ratified ADR constrains backend seam design or platform selection.","source":"docs/adrs/0001-declarative-perception-framework.md, 0002-perception-migration.md, 0003-model-backend-on-dartantic.md"},{"note":"ADR-0000 contains two pending amendments (A1 on registration contract, A2 on community overlap). A1 affects 'leonard_contract registry' — this bead does not change the extension contract. A2 recommends 'adopt Dart-team plumbing' — this bead implements a platform backend, not transport/plumbing.","source":"docs/adrs/0000-ai-decision-register.md A1/A2, both Status: pending"},{"note":"Naming convention per umbrella CLAUDE.md: 'Package names are human faculties/crafts/achievements, never agent-nouns' (e.g., 'typesetting', not 'etcher'). 'UiAutomator2Backend' is a backend (noun describing the service, not an agent-noun), consistent with 'AppiumBackend'. The `leonard_*` package prefix is used correctly.","source":"/Users/nico/development/engineering.memento/CLAUDE.md lines 87-93"},{"note":"The host file bin/leonard_native_host.dart currently always instantiates AppiumBackend, regardless of platform flag. The bead must wire --platform android to select UiAutomator2Backend instead.","source":"packages/leonard_native/bin/leonard_native_host.dart:43-48"},{"note":"No ADR or ratified standard governs the specific W3C WebDriver endpoints or XML parsing strategy for UiAutomator2 vs XCUITest. This is a pure implementation detail within the backend seam.","source":"[implicit: ADRs do not cover backend internals]"},{"note":"The NativeBackend seam (native_backend.dart:1-6) explicitly names this bead's work as 'purely additive': 'AppiumBackend is the first concrete impl; FakeNativeBackend is the test impl; a later UiAutomator2Backend is purely additive.' The seam was designed to host multiple platform-specific backends from the start.","source":"packages/leonard_native/lib/src/native_backend.dart:1-6"},{"note":"lenny-qxx.2 (commit f80ba15, 2026-06-21) established the NativeBackend seam and AppiumBackend for iOS. The architecture was designed for extensibility: NativeBackend.press() already documents platform-specific keys ('iOS: enter/return/done/consent_accept/alert_dismiss; Android additively recognizes back').","source":"packages/leonard_native/lib/src/native_backend.dart:100-151; git history lenny-qxx.2"},{"note":"lenny-qxx.5 (commit 331364a, 2026-07-10) proved the full Auth0 e2e round-trip on iOS with ASWebAuthenticationSession consent handling. All prior milestones (m1-m5) were iOS-only, leaving Android as forward-compatible scaffolding: NativeSnapshot.platform accepts 'android', native.press recognizes 'back', comments flag iOS-vs-Android divergences.","source":"packages/leonard_native/lib/src/appium_backend.dart comments; native_backend.dart comments; git commits 331364a, e2b3e90"},{"note":"The spike design doc (docs/design/leonard-native-appium/backend_skeleton.dart) prototyped both iOS (XCUITest) and Android (UiAutomator2) patterns. It shows platform-branched capabilities (lines 146-163), Android selectors for Auth0 (lines 119-122), keyboard dismiss branching (lines 127-135), and notes the absence of consent alert on Android (lines 181-183).","source":"docs/design/leonard-native-appium/backend_skeleton.dart"},{"note":"AppiumBackend.enterText already branches on platform for readback: FN4 (line 481-487) reads 'attribute/value' for iOS but the comment notes it should read 'attribute/text' for Android. This divergence is scaffolded in comments but not implemented for Android backend.","source":"packages/leonard_native/lib/src/appium_backend.dart:481-487"},{"note":"AppiumBackend._dismissKeyboard (lines 512-533) branches on platform: Android posts to '/session/$_sid/back' (non-fatal), iOS 26 has no Done key (no-op), older iOS taps 'Done'. The Android pattern is ready to lift into a UiAutomator2Backend.","source":"packages/leonard_native/lib/src/appium_backend.dart:512-533"},{"note":"AppiumBackend._isSecureField (lines 496-507) detects SecureTextField only on iOS. Android UiAutomator2 does not have a direct parallel; the bead will need to handle secure field masking differently (likely by inspecting the element's input type or class).","source":"packages/leonard_native/lib/src/appium_backend.dart:496-507"},{"note":"The CLI (bin/leonard_native_host.dart:43-48) currently hardcodes AppiumBackend instantiation. It parses --platform but does not use it to select a backend. The bead must add branching logic: platform=='android' -> UiAutomator2Backend, else AppiumBackend.","source":"packages/leonard_native/bin/leonard_native_host.dart:43-48"},{"note":"The m2-spec through m5-spec documents (docs/design/leonard-native-appium/) anticipated Android handling at every milestone stage. The spec.md (lines 6-8) names FN4 as a hardening requirement: 'iOS reads attribute/value; Android reads attribute/text'. This confirms the dual-path attribute reading was part of the original architecture.","source":"docs/design/leonard-native-appium/spec.md and m2-spec.md through m5-spec.md"},{"note":"NativeSnapshot.platform field (native_snapshot.dart:100-104) is already wired: 'ios | android'. The snapshot is emitted by each backend and available to the extension and tools, so platform-aware logic can branch on it if needed (though the seam keeps platform quirks inside the impl per design).","source":"packages/leonard_native/lib/src/native_snapshot.dart:95-105"},{"note":"The existing unit test (appium_backend_test.dart) covers iOS-specific behavior: masking flag derivation from element TYPE and the alert_dismiss endpoint. These tests will serve as a template for Android UiAutomator2Backend tests, though Android has no exact parallel to SecureTextField.","source":"packages/leonard_native/test/appium_backend_test.dart:1-88"},{"note":"ADR-0006 (genesis/docs/adr/ADR-0006-pull-free-build.md, Status: Accepted) governs the NativeBackend design. The seam comment (native_backend.dart:2-3) explicitly references it: 'ALL device latency (WebDriver round-trips, a11y-tree polling) lives behind it; the extension never touches the device directly (ADR-0006)'. This remains the contract for UiAutomator2Backend.","source":"genesis/docs/adr/ADR-0006-pull-free-build.md; packages/leonard_native/lib/src/native_backend.dart:2-3"},{"note":"The existing AppiumBackend uses http.Client injection for testing (appium_backend_test.dart:44-50), MockClient for unit tests. This pattern is proven and should be reused for UiAutomator2Backend unit tests.","source":"packages/leonard_native/test/appium_backend_test.dart:44-50"},{"note":"NativeExtension (native_extension.dart:23-83) is platform-agnostic: it wraps any NativeBackend impl and exposes the same tools (tap, enter_text, press, swipe) regardless of backend or platform. No changes needed here; the backend swap is transparent to the extension.","source":"packages/leonard_native/lib/src/native_extension.dart:23-83"},{"note":"The pub exports (leonard_native.dart:14) currently export AppiumBackend by name. When UiAutomator2Backend is added, it will need a similar export so the CLI (and tests) can instantiate it.","source":"packages/leonard_native/lib/leonard_native.dart:14-15"},{"note":"The native host e2e test (native_host_e2e_test.dart) already exists and drives the full Auth0 flow on iOS with the FakeNativeBackend. This test structure will provide the template for Android e2e validation, though a live emulator run is required (not just the fake backend) to prove Android.","source":"packages/leonard_native/test/native_host_e2e_test.dart"}],"flags":[],"departures":[{"kind":"decision","standard":"lib/src/native_backend.dart:1-6","quote":"/// The `NativeBackend` seam — the I/O boundary that keeps `buildPerception()` synchronous (ADR-0006). ALL device latency (WebDriver round-trips, a11y-tree polling) lives behind it; the extension never touches the device directly. `AppiumBackend` is the first concrete impl; `FakeNativeBackend` is the test impl; a later `UiAutomator2Backend` is purely additive.","contradiction":"CLI bin/leonard_native_host.dart does not wire --platform android to a UiAutomator2Backend impl. Currently always instantiates AppiumBackend regardless of platform flag, leaving the UiAutomator2Backend seam open.","contradicts":false,"acknowledged":true,"ratified":false,"removesOffence":true,"precedent":""}],"missingLenses":[]}